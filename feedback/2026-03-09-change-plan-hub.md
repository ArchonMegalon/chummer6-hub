# Chummer Run Services - Change Plan

Date: 2026-03-09

## Priority

- Publish a real `Chummer.Play.Contracts` package.
- Canonicalize play transport around the engine mutation model instead of a second semantic session model.
- Extract registry/publication ownership into `chummer-hub-registry`.
- Split media contracts into render-only versus narrative-generation families before `chummer-media-factory`.
- Keep run-services as the orchestrator/policy/session/identity/AI shell after the extractions.
