# Legacy Public Surface

The repository no longer publishes the old GitHub Pages `docs/index.html` surface.

Public front-door ownership now lives in the hosted `chummer.run` shell inside `Chummer.Run.Api`, driven from the local `.codex-design/` mirror.
