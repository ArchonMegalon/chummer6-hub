@model LandingPageViewModel
@{
    ViewData["Title"] = "Chummer — Shadowrun character manager";
    ViewData["Chrome"] = Model.Chrome;
    ViewData["SurfaceClass"] = "surface-landing surface-minimal";
    var publishedLabel = Model.Manifest.PublishedAt.ToUniversalTime().ToString("yyyy-MM-dd");
    var buildLabel = Model.ReleaseExperience.Display.BuildLabel;
    var starterWorkspace = Model.CampaignSpine?.Workspaces.FirstOrDefault();
    var starterFirstPlayableSession = starterWorkspace?.FirstPlayableSession;
}

<section class="minimal-hero" data-homepage-section="hero">
    <div class="minimal-hero__copy">
        <h1>Chummer</h1>
        <p class="minimal-hero__lead">A Shadowrun character manager for building, updating, and bringing clean sheets to the table.</p>
        <div class="minimal-actions" aria-label="Main downloads">
            <a class="button-like button-like--primary" href="/downloads" data-analytics-event="homepage_open_downloads" data-analytics-surface="minimal_home" data-analytics-label="Download Chummer">Download Chummer</a>
        </div>
        <p class="minimal-meta">Windows and Linux. @buildLabel. @publishedLabel.</p>
    </div>
    <figure class="minimal-hero__visual minimal-hero__visual--screenshot">
        <img src="/media/product/chummer-desktop-runner.png" alt="Chummer desktop character sheet" loading="eager" decoding="async" />
    </figure>
</section>

<section class="minimal-section" data-homepage-section="workflow">
    <div class="minimal-section__lead">
        <h2>What it does</h2>
    </div>
    <div class="minimal-three">
        <article>
            <h3>Create</h3>
            <p>Start a character, pick the ruleset, and keep the sheet readable while you work.</p>
        </article>
        <article>
            <h3>Update</h3>
            <p>Track gear, qualities, contacts, and advancement as the campaign changes.</p>
        </article>
        <article>
            <h3>Review</h3>
            <p>Review totals, warnings, and changes before the sheet reaches the table.</p>
        </article>
    </div>
    <figure class="minimal-video">
        <video controls playsinline preload="metadata" poster="/media/promo/chummer6-flagship-promo-poster.png" aria-label="Product video">
            <source src="/media/promo/chummer6-flagship-promo.mp4?v=20260620-audio2" type="video/mp4" />
            <source src="/media/promo/chummer6-flagship-promo.webm" type="video/webm" />
            <track kind="captions" src="/media/promo/chummer6-flagship-promo.vtt" srclang="en" label="English" default />
            Your browser cannot play this video here.
        </video>
    </figure>
</section>

<section class="minimal-section minimal-section--support" data-homepage-section="help" data-public-section="footer">
    <div class="minimal-section__lead">
        <h2>Need something else?</h2>
        <p>Use help for setup and recovery. Use participate for public feedback.</p>
    </div>
    <div class="minimal-actions">
        <a class="button-like button-like--primary" href="/help" data-analytics-event="homepage_open_help" data-analytics-surface="minimal_home" data-analytics-label="Help">Help</a>
        <a class="button-like button-like--ghost" href="/participate" data-analytics-event="homepage_open_participate" data-analytics-surface="minimal_home" data-analytics-label="Participate">Participate</a>
    </div>
</section>

@if (Model.Chrome.Authenticated)
{
    <section class="minimal-section minimal-section--support" data-homepage-section="starter-lane">
        <div class="minimal-section__lead">
            <h2>Next session</h2>
            <p>@(starterFirstPlayableSession?.CampaignStartSummary ?? "Open Home when you are ready to start the first playable session.")</p>
        </div>
        <div class="minimal-actions">
            <a class="button-like button-like--primary" href="/home/work">Open Home</a>
            <a class="button-like button-like--ghost" href="@(starterWorkspace is null ? "/home/work" : $"/account/work/workspaces/{Uri.EscapeDataString(starterWorkspace.WorkspaceId)}#selected-first-playable-session")">Open first playable session</a>
            <a class="button-like button-like--ghost" href="/contact?kind=install_help">Open install support</a>
        </div>
    </section>
}
