@model BlackLedgerFactionHomeViewModel
@{
    ViewData["Title"] = "My Black Ledger faction";
    ViewData["Chrome"] = Model.Chrome;
    ViewData["SurfaceClass"] = "surface-ledger";
}

<section class="trust-page-hero">
    <div class="trust-page-hero__copy">
        <p class="eyebrow">Faction home</p>
        <h1 class="page-title">@Model.Heading</h1>
        <p class="page-copy">@Model.Intro</p>
        <div class="trust-claim__microproof">
            <span>Faction: @Model.Faction.PublicName</span>
            <span>Membership: @Model.Allegiance.MembershipType</span>
            <span>Lock until: @Model.Allegiance.LockUntilUtc?.ToString("yyyy-MM-dd")</span>
        </div>
    </div>
    <div class="stacked-actions">
        <a class="button-like button-like--primary" href="/account/ledger/factions/@Model.Faction.FactionId.Replace('_', '-')">Open my faction</a>
        <a class="button-like button-like--secondary" href="/account/ledger/notifications">Open notifications</a>
        <a class="button-like button-like--secondary" href="/ledger/map#ledger-map">Open command map</a>
    </div>
</section>

@if (Model.NewsreelStatus is not null)
{
    <section class="editorial-block">
        <div class="editorial-block__lead">
            <p class="eyebrow">Turn 1 newsreel</p>
            <h2 class="editorial-title">@Model.NewsreelStatus.StatusLabel</h2>
            <p class="editorial-copy">@Model.NewsreelStatus.Summary</p>
        </div>
        <div class="trust-claim__microproof">
            <span>Policy: @Model.NewsreelStatus.Policy</span>
            <span>Receipts: @Model.NewsreelStatus.ReceiptCount</span>
            <span>Recipients sent: @Model.NewsreelStatus.RecipientCount</span>
        </div>
        <div class="stacked-actions">
            <a class="button-like button-like--secondary" href="@Model.NewsreelStatus.NotificationsHref">Open notification receipts</a>
            <a class="button-like button-like--secondary" href="@Model.NewsreelStatus.TurnHref">Open Turn 1</a>
        </div>
    </section>
}

<section class="editorial-block">
    <div class="editorial-block__lead">
        <p class="eyebrow">Identity loop</p>
        <h2 class="editorial-title">Welcome kit and first task</h2>
        <p class="editorial-copy">Faction welcome kit, badge, runner passport stamp, rank posture, and weekly dispatch hooks all start here.</p>
    </div>
    <div class="route-choice-grid">
        @foreach (var item in Model.WelcomeKit)
        {
            <article class="route-choice-card">
                <span class="tag">Faction identity</span>
                <p>@item</p>
            </article>
        }
    </div>
</section>

@if (Model.PromoArtifact is not null)
{
    <section class="editorial-block">
        <div class="editorial-block__lead">
            <p class="eyebrow">Faction promo artifact</p>
            <h2 class="editorial-title">Provider-verified fallback</h2>
            <p class="editorial-copy">This onboarding lane stays honest: provider verification is still required, so the faction promo ships as first-party storyboard, static card, and captions.</p>
        </div>
        <div class="route-choice-grid">
            <article class="route-choice-card">
                <span class="tag">@Model.PromoArtifact.ProviderStatus</span>
                <h3>@Model.PromoArtifact.PublicName</h3>
                <p>Render mode: @Model.PromoArtifact.RenderMode</p>
                <div class="route-choice-card__proof">
                    @foreach (var format in Model.PromoArtifact.FormatLabels)
                    {
                        <span>@format</span>
                    }
                </div>
                <div class="stacked-actions">
                    <a class="editorial-strip__action" href="@Model.PromoArtifact.HtmlHref">Open promo brief</a>
                    <a class="editorial-strip__action" href="@Model.PromoArtifact.JsonHref">Open JSON brief</a>
                    <a class="editorial-strip__action" href="@Model.PromoArtifact.CaptionsHref">Open captions</a>
                </div>
            </article>
        </div>
    </section>
}

@if (Model.RecentActionReceipts.Count > 0)
{
    <section class="editorial-block">
        <div class="editorial-block__lead">
            <p class="eyebrow">Recent receipts</p>
            <h2 class="editorial-title">Action trail</h2>
            <p class="editorial-copy">Every action needs a receipt.</p>
        </div>
        <div class="route-choice-grid">
            @foreach (var receipt in Model.RecentActionReceipts)
            {
                <article class="route-choice-card">
                    <span class="tag">@receipt.ActionLabel</span>
                    <h3>@receipt.ReceiptId</h3>
                    <p>@receipt.ResultSummary</p>
                    <a class="editorial-strip__action" href="@receipt.Href">View receipt</a>
                </article>
            }
        </div>
    </section>
}
