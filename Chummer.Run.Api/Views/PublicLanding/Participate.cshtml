@model ParticipatePageViewModel
@{
    ViewData["Title"] = "Participate";
    ViewData["Chrome"] = Model.Chrome;
    ViewData["SurfaceClass"] = "surface-participate";

    string ResolveCardHref(PublicFeatureCardDto card)
        => Model.Chrome.Authenticated
            ? card.RegisteredHref ?? card.FallbackRoute ?? card.Href
            : card.GuestHref ?? card.FallbackRoute ?? card.Href;

    string ResolveCardLabel(PublicFeatureCardDto card, string fallback)
        => card.ActionLabel ?? fallback;
}

<section class="story-guide-tail">
    <p class="eyebrow">Participate</p>
    <h1 class="page-title">Choose how to participate</h1>
    <p class="page-copy">Public feedback stays open. Signed-in contribution stays optional and guided.</p>
</section>

<section class="editorial-block editorial-block--lanes">
    <div class="lane-strip-list">
        <article class="lane-strip">
            <div class="lane-strip__copy">
                <span class="tag">Public</span>
                <h2>Public feedback</h2>
                <p>Report a problem or suggest a future without needing an account.</p>
                <div class="compact-rail">
                    @foreach (var card in Model.PublicLane)
                    {
                        var sectionId = card.Href.Contains('#', StringComparison.Ordinal)
                            ? card.Href[(card.Href.LastIndexOf('#') + 1)..]
                            : card.Id.Replace('_', '-');
                        <article class="compact-rail__item" id="@sectionId">
                            <div class="compact-rail__copy">
                                <h3>@card.Title</h3>
                                <p>@card.Summary</p>
                            </div>
                            <a class="editorial-strip__action" href="@ResolveCardHref(card)">@ResolveCardLabel(card, "Open")</a>
                        </article>
                    }
                </div>
            </div>
        </article>

        <article class="lane-strip">
            <div class="lane-strip__copy">
                <span class="tag tag--amber">Signed in</span>
                <h2>Guided contribution</h2>
                <p>Use the signed-in path when you want to lend focused help through a guided contribution flow.</p>
                <div class="compact-rail">
                    @foreach (var card in Model.SignedInLane)
                    {
                        var sectionId = card.Href.Contains('#', StringComparison.Ordinal)
                            ? card.Href[(card.Href.LastIndexOf('#') + 1)..]
                            : card.Id.Replace('_', '-');
                        <article class="compact-rail__item" id="@sectionId">
                            <div class="compact-rail__copy">
                                <h3>@card.Title</h3>
                                <p>@card.Summary</p>
                            </div>
                            <a class="editorial-strip__action" href="@ResolveCardHref(card)">@ResolveCardLabel(card, "Continue")</a>
                        </article>
                    }
                </div>
            </div>
        </article>
    </div>
</section>
