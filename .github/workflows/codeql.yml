# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
name: "CodeQL"

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]
    # Only run on the following paths / file type changes
    paths:
        - "**.sln"
        - "**.csproj"
        - "**.cs"
        - "**.cshtml"
        - "**.xaml"
        - "**.yml" # Include this to have the action rerun on yml changes

  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main", "master" ]
    # Only run on the following paths / file type changes
    paths:
        - "**.sln"
        - "**.csproj"
        - "**.cs"
        - "**.cshtml"
        - "**.xaml"
        - "**.yml" # Include this to have the action rerun on yml changes
  schedule:
    - cron: '20 15 * * 1'

jobs:
  analyze:
    name: Analyze
    # Runner size impacts CodeQL analysis time. To learn more, please see:
    #   - https://gh.io/recommended-hardware-resources-for-running-codeql
    #   - https://gh.io/supported-runners-and-hardware-resources
    #   - https://gh.io/using-larger-runners
    # Consider using larger runners for possible analysis time improvements.
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'windows-latest' }}
    timeout-minutes: ${{ (matrix.language == 'swift' && 120) || 360 }}
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp' ]
        # CodeQL supports [ 'cpp', 'csharp', 'go', 'java', 'javascript', 'python', 'ruby', 'swift' ]
        # Use only 'java' to analyze code written in Java, Kotlin or both
        # Use only 'javascript' to analyze code written in JavaScript, TypeScript or both
        # Learn more about CodeQL language support at https://aka.ms/codeql-docs/language-support

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Enable Windows long paths
      shell: pwsh
      run: git config --system core.longpaths true

    - name: Checkout core compatibility tree
      uses: actions/checkout@v4
      with:
        repository: ArchonMegalon/chummer6-core
        path: compat/chummer-core-engine
        sparse-checkout: |
          Chummer.Contracts
          Directory.Build.props
          global.json

    - name: Checkout hub registry compatibility tree
      uses: actions/checkout@v4
      with:
        repository: ArchonMegalon/chummer6-hub-registry
        path: compat/chummer-hub-registry
        sparse-checkout: |
          Chummer.Hub.Registry.Contracts
          Chummer.Run.Registry
          Directory.Build.props
          global.json

    - name: Checkout media factory compatibility tree
      uses: actions/checkout@v4
      with:
        repository: ArchonMegalon/chummer6-media-factory
        path: compat/chummer-media-factory
        sparse-checkout: |
          src/Chummer.Media.Contracts
          src/Chummer.Media.Factory.Runtime
          Directory.Build.props
          global.json

    - name: Link compatibility tree layout
      shell: pwsh
      run: |
        $repoRoot = $env:GITHUB_WORKSPACE
        $runRoot = Split-Path -Parent $repoRoot
        $actionsRoot = Split-Path -Parent $runRoot

        $links = @(
          @{ Path = Join-Path $runRoot "chummer-core-engine"; Target = Join-Path $repoRoot "compat/chummer-core-engine" },
          @{ Path = Join-Path $runRoot "chummer-hub-registry"; Target = Join-Path $repoRoot "compat/chummer-hub-registry" }
        )
        foreach ($link in $links) {
          if (-not (Test-Path $link.Path)) {
            New-Item -ItemType Junction -Path $link.Path -Target $link.Target | Out-Null
          }
        }

        $fleetRepos = Join-Path $actionsRoot "fleet/repos"
        New-Item -ItemType Directory -Force -Path $fleetRepos | Out-Null
        $mediaLink = Join-Path $fleetRepos "chummer-media-factory"
        if (-not (Test-Path $mediaLink)) {
          New-Item -ItemType Junction -Path $mediaLink -Target (Join-Path $repoRoot "compat/chummer-media-factory") | Out-Null
        }

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: manual
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file.
        # Prefix the list here with "+" to use these queries and those in the config file.

        # For more details on CodeQL's query packs, refer to: https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
        # queries: security-extended,security-and-quality


    # Autobuild attempts to build any compiled languages (C/C++, C#, Go, Java, or Swift).
    # If this step fails, then you should remove it and run the build manually (see below)
    # - name: Autobuild
    #   uses: github/codeql-action/autobuild@v3

    # ℹ️ Command-line programs to run using the OS shell.
    # 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun

    #   If the Autobuild fails above, remove it and uncomment the following three lines.
    #   modify them (or add more) to build your code if your project, please refer to the EXAMPLE below for guidance.

    - run: |
        echo "Run, Build Application using script"
        dotnet restore ./Chummer.Run.sln
        dotnet build --configuration Release --no-restore ./Chummer.Run.sln

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
