using System.Text.Json;
using Xunit;

namespace Chummer.Tests;

public sealed class VerificationEntryPointTests
{
    [Fact]
    public void AuditComplianceUsesSupportedVerificationScript()
    {
        string scriptPath = RepoPaths.FromRoot("scripts", "audit-compliance.sh");
        string script = File.ReadAllText(scriptPath);

        Assert.Contains("bash scripts/ai/verify.sh", script, StringComparison.Ordinal);
    }

    [Fact]
    public void ParityChecklistGeneratorFailClosesMalformedParityTokens()
    {
        string scriptPath = RepoPaths.FromRoot("scripts", "generate-parity-checklist.sh");
        string script = File.ReadAllText(scriptPath);

        Assert.Contains("normalize_required_token", script, StringComparison.Ordinal);
        Assert.Contains("parse_required_token_list", script, StringComparison.Ordinal);
        Assert.Contains("parse_catalog_token_matches", script, StringComparison.Ordinal);
        Assert.Contains("fail_on_unacknowledged_catalog_only", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_PARITY_DESKTOP_DIALOG_FACTORY_PATH", script, StringComparison.Ordinal);
        Assert.Contains("acknowledgedCatalogOnlyTabs", script, StringComparison.Ordinal);
        Assert.Contains("acknowledgedCatalogOnlyWorkspaceActions", script, StringComparison.Ordinal);
        Assert.Contains("acknowledgedDialogFactoryOnlyDesktopControls", script, StringComparison.Ordinal);
        Assert.Contains("desktopControls", script, StringComparison.Ordinal);
        Assert.Contains("must not contain blank token values", script, StringComparison.Ordinal);
        Assert.Contains("contains whitespace-padded token", script, StringComparison.Ordinal);
        Assert.Contains("contains non-canonical token", script, StringComparison.Ordinal);
        Assert.Contains("contains duplicate normalized token", script, StringComparison.Ordinal);
        Assert.Contains("is missing required acknowledged catalog-only", script, StringComparison.Ordinal);
        Assert.Contains("fail_on_missing_required_legacy_ids", script, StringComparison.Ordinal);
        Assert.Contains("surface_label=\"tab\"", script, StringComparison.Ordinal);
        Assert.Contains("surface_label=\"workspace action\"", script, StringComparison.Ordinal);
        Assert.Contains("is missing required legacy desktop control ids", script, StringComparison.Ordinal);
        Assert.Contains("dialog-factory-only desktop control", script, StringComparison.Ordinal);
        Assert.Contains("no longer catalog-only", script, StringComparison.Ordinal);
    }

    [Fact]
    public void AuditUiParityUsesActiveParityGeneratorInsteadOfRetiredLegacyShellFiles()
    {
        string scriptPath = RepoPaths.FromRoot("scripts", "audit-ui-parity.sh");
        string script = File.ReadAllText(scriptPath);

        Assert.Contains("scripts/generate-parity-checklist.sh", script, StringComparison.Ordinal);
        Assert.Contains("docs/PARITY_CHECKLIST.md", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_UI_PUBLISHED_DIR", script, StringComparison.Ordinal);
        Assert.Contains("\"$ROOT/../chummer6-ui\"", script, StringComparison.Ordinal);
        Assert.Contains("FALLBACK_UI_REPO_ROOT=\"$ROOT/../chummer6-ui-finish\"", script, StringComparison.Ordinal);
        Assert.Contains("LEGACY_UI_REPO_ROOT=\"$ROOT/../chummer-presentation-clean\"", script, StringComparison.Ordinal);
        Assert.Contains("OLDER_UI_REPO_ROOT=\"$ROOT/../chummer-presentation\"", script, StringComparison.Ordinal);
        Assert.Contains("LEGACY_UI_PUBLISHED_DIR=\"$LEGACY_UI_REPO_ROOT/.codex-studio/published\"", script, StringComparison.Ordinal);
        Assert.Contains("OLDER_UI_PUBLISHED_DIR=\"$OLDER_UI_REPO_ROOT/.codex-studio/published\"", script, StringComparison.Ordinal);
        Assert.Contains("resolve_receipt_path", script, StringComparison.Ordinal);
        Assert.Contains("DESKTOP_WORKFLOW_EXECUTION_GATE.generated.json", script, StringComparison.Ordinal);
        Assert.Contains("DESKTOP_VISUAL_FAMILIARITY_EXIT_GATE.generated.json", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_UI_PARITY_RELEASE_CHANNEL_PATH", script, StringComparison.Ordinal);
        Assert.Contains("required executable receipt is missing", script, StringComparison.Ordinal);
        Assert.Contains("status must be pass/passed/ready", script, StringComparison.Ordinal);
        Assert.Contains("generatedAt/generated_at is missing", script, StringComparison.Ordinal);
        Assert.Contains("proof_freshness_max_age_seconds", script, StringComparison.Ordinal);
        Assert.Contains("proof_freshness_max_future_skew_seconds", script, StringComparison.Ordinal);
        Assert.Contains("generatedAt/generated_at is stale", script, StringComparison.Ordinal);
        Assert.Contains("generatedAt/generated_at is in the future", script, StringComparison.Ordinal);
        Assert.Contains("required_workflow_family_ids", script, StringComparison.Ordinal);
        Assert.Contains("missing_required_workflow_family_ids", script, StringComparison.Ordinal);
        Assert.Contains("flagship_required_desktop_heads", script, StringComparison.Ordinal);
        Assert.Contains("flagship_missing_or_not_ready_desktop_heads", script, StringComparison.Ordinal);
        Assert.Contains("flagship_missing_canonical_required_desktop_heads", script, StringComparison.Ordinal);
        Assert.Contains("flagship_head_missing_contract_markers", script, StringComparison.Ordinal);
        Assert.Contains("flagship_head_contract_marker_statuses", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_receipt_exists", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_channel_id", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_version", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_path", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_generated_at", script, StringComparison.Ordinal);
        Assert.Contains("release-channel receipt status must be pass/passed/ready/published/unpublished", script, StringComparison.Ordinal);
        Assert.Contains("release-channel channel id drifts from nested receipt", script, StringComparison.Ordinal);
        Assert.Contains("release-channel version drifts from nested receipt", script, StringComparison.Ordinal);
        Assert.Contains("release-channel generated_at drifts from nested receipt generatedAt", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt generatedAt is stale", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt generatedAt is in the future", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof is required", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.status must be pass/passed/ready", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof has unexpected keys", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate must be an object", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.status must be pass/passed/ready", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.generatedAt is stale", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.generatedAt is in the future", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.defaultKeyCount must be an integer", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.defaultKeyCount must be positive", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate has unexpected keys", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.shippingLocales must equal required flagship locales", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.acceptanceGates is missing required gate ids", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.domainCoverage is missing required domains", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.localeDomainCoverage has unexpected locales", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.localeSummary must have zero untranslated keys", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.uiLocalizationReleaseGate.localeSummary must preserve canonical shipping locale ordering", script, StringComparison.Ordinal);
        Assert.Contains("alias values drift between", script, StringComparison.Ordinal);
        Assert.Contains("must be an ISO timestamp", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_UI_PARITY_RELEASE_PROOF_MAX_AGE_SECONDS", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_RELEASE_PROOF_MAX_AGE_SECONDS", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_UI_PARITY_RELEASE_PROOF_MAX_FUTURE_SKEW_SECONDS", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_RELEASE_PROOF_MAX_FUTURE_SKEW_SECONDS", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.generatedAt is stale", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.generatedAt is in the future", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_UI_PARITY_ALLOWED_RELEASE_PROOF_BASE_URLS", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_ALLOWED_RELEASE_PROOF_BASE_URLS", script, StringComparison.Ordinal);
        Assert.Contains("must use canonical origin form with no trailing slash", script, StringComparison.Ordinal);
        Assert.Contains("must match an allowed canonical release origin", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.journeysPassed is missing required baseline journey ids", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.journeysPassed declares unexpected journey ids", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.journeysPassed must preserve canonical baseline journey ordering", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.journeysPassed must use canonical lowercase journey ids", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.journeysPassed must use canonical journey id tokens", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.proofRoutes is missing required flagship routes", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.proofRoutes declares unexpected flagship routes", script, StringComparison.Ordinal);
        Assert.Contains("release-channel nested receipt releaseProof.proofRoutes must preserve canonical flagship route ordering", script, StringComparison.Ordinal);
        Assert.Contains("must use canonical lowercase route casing", script, StringComparison.Ordinal);
        Assert.Contains("must not include percent-encoded or escaped path characters", script, StringComparison.Ordinal);
        Assert.Contains("workflow_parity_receipt_channel_ids", script, StringComparison.Ordinal);
        Assert.Contains("milestone-2 workflow/visual release-channel ids drift", script, StringComparison.Ordinal);
        Assert.Contains("milestone-2 workflow/visual release-channel versions drift", script, StringComparison.Ordinal);
        Assert.Contains("milestone-2 workflow/visual release-channel nested receipt paths drift", script, StringComparison.Ordinal);
        Assert.Contains("milestone-2 workflow/visual release-channel generated_at drift", script, StringComparison.Ordinal);
        Assert.Contains("flagship_gate.headProofs.status_malformed_entries", script, StringComparison.Ordinal);
        Assert.Contains("flagship_gate.headProofs.status_non_canonical_keys", script, StringComparison.Ordinal);
        Assert.Contains("flagship_gate.headProofs.status_duplicate_normalized_keys", script, StringComparison.Ordinal);
        Assert.Contains("workflow_execution_missing_receipts", script, StringComparison.Ordinal);
        Assert.Contains("workflow_execution_weak_receipts", script, StringComparison.Ordinal);
        Assert.Contains("workflow_family_missing_receipts", script, StringComparison.Ordinal);
        Assert.Contains("workflow_family_failing_receipts", script, StringComparison.Ordinal);
        Assert.Contains("workflow_family_receipt_count_checked", script, StringComparison.Ordinal);
        Assert.Contains("workflow_execution_receipt_count_checked", script, StringComparison.Ordinal);
        Assert.Contains("missing_required_workflow_family_audit_tests", script, StringComparison.Ordinal);
        Assert.Contains("sr4_workflow_parity_status", script, StringComparison.Ordinal);
        Assert.Contains("sr6_workflow_parity_status", script, StringComparison.Ordinal);
        Assert.Contains("chummer5a_workflow_parity_status", script, StringComparison.Ordinal);
        Assert.Contains("sr4_sr6_frontier_status", script, StringComparison.Ordinal);
        Assert.Contains("workflow_parity_proof_max_age_seconds", script, StringComparison.Ordinal);
        Assert.Contains("evidence path is missing", script, StringComparison.Ordinal);
        Assert.Contains("resolve_nested_receipt_path", script, StringComparison.Ordinal);
        Assert.Contains("nested receipt generatedAt is stale", script, StringComparison.Ordinal);
        Assert.Contains("nested receipt generatedAt is in the future", script, StringComparison.Ordinal);
        Assert.Contains("generated_at drifts from nested receipt generatedAt", script, StringComparison.Ordinal);
        Assert.Contains("workflow receipt declares unexpected milestone-2 family ids", script, StringComparison.Ordinal);
        Assert.Contains("workflow receipt required_workflow_family_ids must preserve canonical milestone-2 family ordering", script, StringComparison.Ordinal);
        Assert.Contains("evidence age exceeds allowed freshness window", script, StringComparison.Ordinal);
        Assert.Contains("evidence generated_at is stale", script, StringComparison.Ordinal);
        Assert.Contains("evidence generated_at is in the future", script, StringComparison.Ordinal);
        Assert.Contains("load_required_family_ids", script, StringComparison.Ordinal);
        Assert.Contains("SR4_WORKFLOW_LEDGER_PATH", script, StringComparison.Ordinal);
        Assert.Contains("SR6_WORKFLOW_LEDGER_PATH", script, StringComparison.Ordinal);
        Assert.Contains("requiredFamilies", script, StringComparison.Ordinal);
        Assert.Contains("required_legacy_interaction_keys", script, StringComparison.Ordinal);
        Assert.Contains("missing_required_legacy_interaction_keys", script, StringComparison.Ordinal);
        Assert.Contains("reports non-pass flagship head contract markers", script, StringComparison.Ordinal);
        Assert.Contains("required_tests", script, StringComparison.Ordinal);
        Assert.Contains("must not include leading/trailing whitespace", script, StringComparison.Ordinal);
        Assert.Contains("must not be blank", script, StringComparison.Ordinal);
        Assert.Contains("must not contain duplicate ids", script, StringComparison.Ordinal);
        Assert.Contains("Desktop_shell_preserves_classic_dense_three_pane_workbench_posture", script, StringComparison.Ordinal);
        Assert.Contains("Gear_builder_preserves_familiar_browse_detail_confirm_rhythm", script, StringComparison.Ordinal);
        Assert.Contains("Cyberware_and_cyberlimb_builder_preserve_legacy_dialog_familiarity_cues", script, StringComparison.Ordinal);
        Assert.Contains("Contacts_diary_and_support_routes_execute_with_public_path_visibility", script, StringComparison.Ordinal);
        Assert.Contains("Loaded_runner_main_window_routes_navigation_palette_dialog_and_quick_action_surfaces_end_to_end", script, StringComparison.Ordinal);
        Assert.Contains("missing required milestone-2 visual tests", script, StringComparison.Ordinal);
        Assert.Contains("declares unexpected milestone-2 visual tests", script, StringComparison.Ordinal);
        Assert.Contains("must preserve canonical milestone-2 visual test ordering", script, StringComparison.Ordinal);
        Assert.Contains("required_screenshots", script, StringComparison.Ordinal);
        Assert.Contains("missing required milestone-2 screenshots", script, StringComparison.Ordinal);
        Assert.Contains("declares unexpected milestone-2 screenshots", script, StringComparison.Ordinal);
        Assert.Contains("must preserve canonical milestone-2 screenshot ordering", script, StringComparison.Ordinal);
        Assert.Contains("screenshot_dir is missing", script, StringComparison.Ordinal);
        Assert.Contains("screenshot_dir does not exist", script, StringComparison.Ordinal);
        Assert.Contains("screenshot_timestamps must be a JSON object", script, StringComparison.Ordinal);
        Assert.Contains("screenshot timestamp drifts from on-disk file mtime", script, StringComparison.Ordinal);
        Assert.Contains("screenshot_receipt_skew_max_seconds", script, StringComparison.Ordinal);
        Assert.Contains("required screenshot file is missing on disk", script, StringComparison.Ordinal);
        Assert.Contains("missing_screenshots", script, StringComparison.Ordinal);
        Assert.Contains("missing_theme_tokens", script, StringComparison.Ordinal);
        Assert.Contains("flagship_theme_readability_contrast", script, StringComparison.Ordinal);
        Assert.Contains("runtime_backed_shell_menu", script, StringComparison.Ordinal);
        Assert.Contains("runtime_backed_menu_bar_labels", script, StringComparison.Ordinal);
        Assert.Contains("runtime_backed_toolstrip_actions", script, StringComparison.Ordinal);
        Assert.Contains("runtime_backed_tab_panel_only_header", script, StringComparison.Ordinal);
        Assert.Contains("runtime_backed_clickable_primary_menus", script, StringComparison.Ordinal);
        Assert.Contains("loaded_runner_tab_strip_control_present", script, StringComparison.Ordinal);
        Assert.Contains("loaded_runner_tab_posture_control_present", script, StringComparison.Ordinal);
        Assert.Contains("flagship theme/readability proof is not pass-ready", script, StringComparison.Ordinal);
        Assert.Contains("invalid_screenshots", script, StringComparison.Ordinal);
        Assert.Contains("undersized_screenshots", script, StringComparison.Ordinal);
        Assert.Contains("stale_screenshots", script, StringComparison.Ordinal);
        Assert.Contains("screenshots_older_than_flagship_receipt", script, StringComparison.Ordinal);
        Assert.Contains("runtimeBackedLegacyWorkbench", script, StringComparison.Ordinal);
        Assert.Contains("legacyDenseBuilderRhythm", script, StringComparison.Ordinal);
        Assert.Contains("legacyBrowseDetailConfirmRhythm", script, StringComparison.Ordinal);
        Assert.Contains("legacyContactsDiaryRhythm", script, StringComparison.Ordinal);
        Assert.Contains("legacyMagicWorkflowRhythm", script, StringComparison.Ordinal);
        Assert.Contains("legacyDiaryWorkflowRhythm", script, StringComparison.Ordinal);
        Assert.Contains("declares unexpected milestone-2 interaction keys", script, StringComparison.Ordinal);
        Assert.Contains("must preserve canonical milestone-2 interaction key ordering", script, StringComparison.Ordinal);
        Assert.Contains("legacy_familiarity_bridge", script, StringComparison.Ordinal);
        Assert.Contains("required_visual_status_fields", script, StringComparison.Ordinal);
        Assert.Contains("{label} proof is not pass-ready", script, StringComparison.Ordinal);
        Assert.Contains("Parity audit passed", script, StringComparison.Ordinal);
        Assert.DoesNotContain("Chummer.Web/wwwroot/index.html", script, StringComparison.Ordinal);
        Assert.DoesNotContain("Chummer/Forms/ChummerMainForm.Designer.cs", script, StringComparison.Ordinal);
        Assert.DoesNotContain("required files missing", script, StringComparison.Ordinal);
    }

    [Fact]
    public void VerifyEntrypointRunsUiParityAudit()
    {
        string scriptPath = RepoPaths.FromRoot("scripts", "ai", "verify.sh");
        string script = File.ReadAllText(scriptPath);

        Assert.Contains("bash scripts/audit-ui-parity.sh", script, StringComparison.Ordinal);
        Assert.Contains("run_ui_parity_audit_with_workflow_gate_retry", script, StringComparison.Ordinal);
        Assert.Contains("run_gate_materializer_script", script, StringComparison.Ordinal);
        Assert.Contains("WORKFLOW_GATE_DRIFT_RETRY_MARKER_PREFIX", script, StringComparison.Ordinal);
        Assert.Contains("WORKFLOW_EVIDENCE_TIMESTAMP_DRIFT_MARKER_PREFIX", script, StringComparison.Ordinal);
        Assert.Contains("WORKFLOW_EVIDENCE_TIMESTAMP_DRIFT_MARKER_SUFFIX", script, StringComparison.Ordinal);
        Assert.Contains("WORKFLOW_RELEASE_CHANNEL_TIMESTAMP_DRIFT_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("VISUAL_RELEASE_CHANNEL_TIMESTAMP_DRIFT_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("UI_VISUAL_FAMILIARITY_GATE_MATERIALIZER", script, StringComparison.Ordinal);
        Assert.Contains("materialize-desktop-visual-familiarity-exit-gate.sh", script, StringComparison.Ordinal);
        Assert.Contains("VISUAL_REQUIRED_TESTS_ORDER_DRIFT_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("VISUAL_INTERACTION_KEYS_ORDER_DRIFT_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("VISUAL_SCREENSHOTS_ORDER_DRIFT_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("VISUAL_MISSING_INTERACTION_KEYS_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("milestone-2 workflow/visual release-channel ", script, StringComparison.Ordinal);
        Assert.Contains("evidence generated_at drifts from nested receipt generatedAt", script, StringComparison.Ordinal);
        Assert.Contains("materialize-desktop-workflow-execution-gate.sh", script, StringComparison.Ordinal);
        Assert.Contains("verify note: rematerializing desktop workflow execution gate after milestone-2 release-channel drift.", script, StringComparison.Ordinal);
        Assert.Contains("verify note: rematerializing desktop visual familiarity exit gate after canonical ordering drift.", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_DESKTOP_VISUAL_SKIP_RELEASE_GATE_LOCK_WAIT=1", script, StringComparison.Ordinal);
        Assert.Contains("is missing or not readable", script, StringComparison.Ordinal);
        Assert.Contains("sync_workflow_evidence_timestamps_from_nested_receipts", script, StringComparison.Ordinal);
        Assert.Contains("sync_ui_release_channel_evidence_timestamps_from_nested_receipts", script, StringComparison.Ordinal);
        Assert.Contains("verify note: syncing workflow parity evidence timestamps from nested workflow receipts.", script, StringComparison.Ordinal);
        Assert.Contains("verify note: syncing UI release-channel evidence timestamps from nested release-channel receipt.", script, StringComparison.Ordinal);
        Assert.Contains("sync_release_channel_localization_gate_timestamp_from_ui_receipt", script, StringComparison.Ordinal);
        Assert.Contains("UI_LOCALIZATION_RELEASE_GATE.generated.json", script, StringComparison.Ordinal);
        Assert.Contains("UI_LOCALIZATION_GATE_TIMESTAMP_STALE_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("UI_LOCALIZATION_GATE_TIMESTAMP_STALE_ALIAS_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("UI_LOCALIZATION_GATE_TIMESTAMP_FUTURE_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("UI_LOCALIZATION_GATE_TIMESTAMP_FUTURE_ALIAS_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("verify note: syncing release-channel nested UI localization gate timestamp from canonical UI localization receipt.", script, StringComparison.Ordinal);
        Assert.Contains("RELEASE_PROOF_TIMESTAMP_STALE_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("RELEASE_PROOF_TIMESTAMP_STALE_ALIAS_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("RELEASE_PROOF_TIMESTAMP_FUTURE_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("RELEASE_PROOF_TIMESTAMP_FUTURE_ALIAS_MARKER", script, StringComparison.Ordinal);
        Assert.Contains("sync_release_channel_proof_timestamp_from_release_channel_receipt", script, StringComparison.Ordinal);
        Assert.Contains("verify note: syncing release-channel nested release proof timestamp from canonical release-channel generated_at.", script, StringComparison.Ordinal);
        Assert.Contains("release-channel generated_at/generatedAt is missing", script, StringComparison.Ordinal);
        Assert.Contains("releaseProof.uiLocalizationReleaseGate", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_path", script, StringComparison.Ordinal);
        Assert.Contains("release_channel_fixture_path", script, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_UI_PARITY_RELEASE_CHANNEL_PATH", script, StringComparison.Ordinal);
        Assert.Contains("DESKTOP_WORKFLOW_EXECUTION_GATE.generated.json", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.baseUrl outside allowed canonical release origins", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.baseUrl and releaseProof.base_url", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.proofRoutes and releaseProof.proof_routes", script, StringComparison.Ordinal);
        Assert.Contains("reject whitespace-padded releaseProof.proof_routes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.journeysPassed and releaseProof.journeys_passed", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical token shape in releaseProof.journeys_passed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical releaseProof.baseUrl origin casing/trailing slash", script, StringComparison.Ordinal);
        Assert.Contains("reject missing releaseProof.baseUrl origin", script, StringComparison.Ordinal);
        Assert.Contains("reject non-http(s) releaseProof.baseUrl schemes", script, StringComparison.Ordinal);
        Assert.Contains("reject non-http(s) releaseProof.base_url alias schemes", script, StringComparison.Ordinal);
        Assert.Contains("reject non-origin releaseProof.baseUrl path/query segments", script, StringComparison.Ordinal);
        Assert.Contains("reject non-origin releaseProof.base_url alias path/query segments", script, StringComparison.Ordinal);
        Assert.Contains("reject hostless releaseProof.baseUrl origins", script, StringComparison.Ordinal);
        Assert.Contains("reject hostless releaseProof.base_url alias origins", script, StringComparison.Ordinal);
        Assert.Contains("reject userinfo credentials in releaseProof.baseUrl origins", script, StringComparison.Ordinal);
        Assert.Contains("reject userinfo credentials in releaseProof.base_url alias origins", script, StringComparison.Ordinal);
        Assert.Contains("reject whitespace-padded releaseProof.baseUrl values", script, StringComparison.Ordinal);
        Assert.Contains("reject whitespace-padded releaseProof.base_url alias values", script, StringComparison.Ordinal);
        Assert.Contains("reject percent-encoded releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject query/fragment releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject escaped-path releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject whitespace-padded releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject non-slash-led releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject dot-segment traversal releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical uppercase releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject empty-segment releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject duplicate-normalized releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject missing required releaseProof.proofRoutes flagship routes", script, StringComparison.Ordinal);
        Assert.Contains("reject unexpected releaseProof.proofRoutes flagship routes", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical releaseProof.proofRoutes ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject duplicate releaseProof.journeysPassed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject missing required releaseProof.journeysPassed baseline journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject unexpected releaseProof.journeysPassed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical releaseProof.journeysPassed ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical lowercase releaseProof.journeysPassed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical token shape in releaseProof.journeysPassed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject non-passing releaseProof.status values", script, StringComparison.Ordinal);
        Assert.Contains("reject non-object releaseProof.uiLocalizationReleaseGate payloads", script, StringComparison.Ordinal);
        Assert.Contains("reject non-passing releaseProof.uiLocalizationReleaseGate.status values", script, StringComparison.Ordinal);
        Assert.Contains("reject missing releaseProof.uiLocalizationReleaseGate.generatedAt timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject invalid-format releaseProof.uiLocalizationReleaseGate.generatedAt timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject invalid-format releaseProof.uiLocalizationReleaseGate.generated_at timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject stale releaseProof.uiLocalizationReleaseGate.generatedAt timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject stale releaseProof.uiLocalizationReleaseGate.generated_at timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.uiLocalizationReleaseGate.generatedAt and releaseProof.uiLocalizationReleaseGate.generated_at", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.uiLocalizationReleaseGate.generated_at timestamps with excessive future skew", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.uiLocalizationReleaseGate.generatedAt timestamps with excessive future skew", script, StringComparison.Ordinal);
        Assert.Contains("reject missing releaseProof.uiLocalizationReleaseGate payloads", script, StringComparison.Ordinal);
        Assert.Contains("reject non-integer releaseProof.uiLocalizationReleaseGate.defaultKeyCount values", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.uiLocalizationReleaseGate.defaultKeyCount and releaseProof.uiLocalizationReleaseGate.default_key_count", script, StringComparison.Ordinal);
        Assert.Contains("reject non-passing releaseProof.uiLocalizationReleaseGate.explicitFallbackRuntime status", script, StringComparison.Ordinal);
        Assert.Contains("reject non-passing releaseProof.uiLocalizationReleaseGate.signoffSmokeRunnerStatus status", script, StringComparison.Ordinal);
        Assert.Contains("reject non-passing releaseProof.uiLocalizationReleaseGate.signoff_smoke_runner_status alias status", script, StringComparison.Ordinal);
        Assert.Contains("reject incomplete releaseProof.uiLocalizationReleaseGate.shippingLocales flagship locale sets", script, StringComparison.Ordinal);
        Assert.Contains("reject incomplete releaseProof.uiLocalizationReleaseGate.acceptanceGates baseline ids", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical releaseProof.uiLocalizationReleaseGate.acceptanceGates ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject non-passing releaseProof.uiLocalizationReleaseGate.localeDomainCoverage locale/domain statuses", script, StringComparison.Ordinal);
        Assert.Contains("reject unexpected releaseProof.uiLocalizationReleaseGate.localeDomainCoverage locales", script, StringComparison.Ordinal);
        Assert.Contains("reject non-integer releaseProof.uiLocalizationReleaseGate.blockingFindingsCount values", script, StringComparison.Ordinal);
        Assert.Contains("reject non-integer releaseProof.uiLocalizationReleaseGate.translationBacklogFindingsCount values", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.uiLocalizationReleaseGate.blockingFindings length/count mismatches", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.uiLocalizationReleaseGate.translationBacklogFindings length/count mismatches", script, StringComparison.Ordinal);
        Assert.Contains("reject non-zero releaseProof.uiLocalizationReleaseGate.localeSummary untranslatedKeyCount values", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical releaseProof.uiLocalizationReleaseGate.localeSummary ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject unexpected releaseProof.uiLocalizationReleaseGate keys", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.uiLocalizationReleaseGate and releaseProof.ui_localization_release_gate", script, StringComparison.Ordinal);
        Assert.Contains("reject unexpected releaseProof keys", script, StringComparison.Ordinal);
        Assert.Contains("reject missing releaseProof payloads", script, StringComparison.Ordinal);
        Assert.Contains("reject non-array releaseProof.journeysPassed payloads", script, StringComparison.Ordinal);
        Assert.Contains("reject non-array releaseProof.proofRoutes payloads", script, StringComparison.Ordinal);
        Assert.Contains("reject non-string releaseProof.journeysPassed entries", script, StringComparison.Ordinal);
        Assert.Contains("reject non-string releaseProof.proofRoutes entries", script, StringComparison.Ordinal);
        Assert.Contains("reject whitespace-padded releaseProof.journeysPassed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject blank releaseProof.journeysPassed journey ids", script, StringComparison.Ordinal);
        Assert.Contains("reject missing releaseProof.generatedAt timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject conflicting alias values between releaseProof.generatedAt and releaseProof.generated_at", script, StringComparison.Ordinal);
        Assert.Contains("reject stale releaseProof.generatedAt timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject stale releaseProof.generated_at timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject invalid-format releaseProof.generatedAt timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject invalid-format releaseProof.generated_at timestamps", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.generated_at timestamps with excessive future skew", script, StringComparison.Ordinal);
        Assert.Contains("reject releaseProof.generatedAt timestamps with excessive future skew", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical required_workflow_family_ids ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject unexpected required_workflow_family_ids values", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical required_tests ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical required_legacy_interaction_keys ordering", script, StringComparison.Ordinal);
        Assert.Contains("reject non-canonical required_screenshots ordering", script, StringComparison.Ordinal);
    }

    [Fact]
    public void ParityOracleTokenListsUseCanonicalStringIds()
    {
        string oraclePath = RepoPaths.FromRoot("docs", "PARITY_ORACLE.json");
        using JsonDocument oracle = JsonDocument.Parse(File.ReadAllText(oraclePath));
        JsonElement root = oracle.RootElement;

        Assert.Equal(JsonValueKind.Object, root.ValueKind);
        AssertCanonicalTokenArray(root, "tabs");
        AssertCanonicalTokenArray(root, "workspaceActions");
        AssertCanonicalTokenArray(root, "acknowledgedCatalogOnlyTabs");
        AssertCanonicalTokenArray(root, "acknowledgedCatalogOnlyWorkspaceActions");
        AssertCanonicalTokenArray(root, "acknowledgedDialogFactoryOnlyDesktopControls");
        AssertCanonicalTokenArray(root, "desktopControls");
    }

    private static void AssertCanonicalTokenArray(JsonElement root, string propertyName)
    {
        Assert.True(root.TryGetProperty(propertyName, out JsonElement values), $"{propertyName} must exist");
        Assert.Equal(JsonValueKind.Array, values.ValueKind);

        HashSet<string> normalized = new(StringComparer.Ordinal);
        int index = 0;
        foreach (JsonElement value in values.EnumerateArray())
        {
            Assert.Equal(JsonValueKind.String, value.ValueKind);

            string token = value.GetString() ?? string.Empty;
            Assert.False(string.IsNullOrWhiteSpace(token), $"{propertyName}[{index}] must not be blank");
            Assert.Equal(token.Trim(), token);
            Assert.Equal(token.ToLowerInvariant(), token);

            string normalizedToken = token.ToLowerInvariant();
            Assert.True(
                normalized.Add(normalizedToken),
                $"{propertyName}[{index}] duplicates normalized token '{token}'");
            index++;
        }
    }

    [Fact]
    public void SupportVerificationGuardAvoidsUnmatchedInstallFallback()
    {
        string presenterPath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "Support", "SupportCasePresentationService.cs");
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "SupportCasesController.cs");

        string presenter = File.ReadAllText(presenterPath);
        string controller = File.ReadAllText(controllerPath);

        Assert.Contains("best is not null && best.Score > 0", presenter, StringComparison.Ordinal);
        Assert.Contains("AllowsReporterVerification", controller, StringComparison.Ordinal);
        Assert.Contains("presented.CanVerifyFix", controller, StringComparison.Ordinal);
    }

    [Fact]
    public void SignedInTrustProjectionSuppressesIdentityOutages()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string controller = File.ReadAllText(controllerPath);

        Assert.Contains("TryGetOptionalPublicSurfaceSubjectAsync", controller, StringComparison.Ordinal);
    }

    [Fact]
    public void SignedInReleaseUploadHandoffIsPublishedFromPortal()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string viewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "ReleaseUpload.cshtml");
        string bootstrapPath = RepoPaths.FromRoot("Chummer.Run.Api", "wwwroot", "artifacts", "mac-codex-release-pipeline", "bootstrap.sh");
        string wrapperPath = RepoPaths.FromRoot("scripts", "run-mac-release-bootstrap.sh");
        string maintenanceReadmePath = RepoPaths.FromRoot("..", "chummer-design", "products", "chummer", "maintenance", "MAC_CODEX_RELEASE_TO_CHUMMER_RUN.md");
        string presentationRunbookPath = RepoPaths.FromRoot("..", "chummer-presentation", "docs", "MAC_CODEX_RELEASE_TO_CHUMMER_RUN.md");
        string selfHostedRunbookPath = RepoPaths.FromRoot("docs", "SELF_HOSTED_DOWNLOADS_RUNBOOK.md");
        string publicReadmePath = RepoPaths.FromRoot("Chummer.Run.Api", "wwwroot", "artifacts", "mac-codex-release-pipeline", "readme.md");

        string controller = File.ReadAllText(controllerPath);
        string viewModel = File.ReadAllText(viewModelPath);
        string view = File.ReadAllText(viewPath);
        string bootstrap = File.ReadAllText(bootstrapPath);
        string wrapper = File.ReadAllText(wrapperPath);
        string maintenanceReadme = File.ReadAllText(maintenanceReadmePath);
        string presentationRunbook = File.ReadAllText(presentationRunbookPath);
        string selfHostedRunbook = File.ReadAllText(selfHostedRunbookPath);
        string publicReadme = File.ReadAllText(publicReadmePath);

        Assert.Contains("/downloads/release-upload", controller, StringComparison.Ordinal);
        Assert.Contains("/downloads/release-upload/bootstrap.sh", controller, StringComparison.Ordinal);
        Assert.Contains("set -o pipefail", controller, StringComparison.Ordinal);
        Assert.Contains("curl -fsSL", controller, StringComparison.Ordinal);
        Assert.Contains("TMP_BOOTSTRAP_SCRIPT", controller, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_BOOTSTRAP_EXPECTED_SHA256", controller, StringComparison.Ordinal);
        Assert.Contains("ReleaseUploadTicketService", controller, StringComparison.Ordinal);
        Assert.Contains("keeps it off the command line", controller, StringComparison.Ordinal);
        Assert.Contains("ReleaseUploadPageViewModel", viewModel, StringComparison.Ordinal);
        Assert.Contains("HandoffCode", viewModel, StringComparison.Ordinal);
        Assert.Contains("Signed-in release upload", view, StringComparison.Ordinal);
        Assert.Contains("source of truth for release upload in Chummer", view, StringComparison.Ordinal);
        Assert.Contains("recovery-aware verification path", view, StringComparison.Ordinal);
        Assert.Contains("recovery-only code", view, StringComparison.Ordinal);
        Assert.Contains("Paste this only when the bootstrap asks", view, StringComparison.Ordinal);
        Assert.DoesNotContain("claim-code return", view, StringComparison.OrdinalIgnoreCase);
        Assert.Contains("CHUMMER_RELEASE_UPLOAD_TOKEN", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_RELEASE_UPLOAD_TICKET", bootstrap, StringComparison.Ordinal);
        Assert.Contains("ensure_release_upload_token", bootstrap, StringComparison.Ordinal);
        Assert.Contains("bootstrap integrity verified", bootstrap, StringComparison.Ordinal);
        Assert.DoesNotContain("staged upload token:", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_RELEASE_UPLOAD_URL", bootstrap, StringComparison.Ordinal);
        Assert.Contains("-p:UseChummerEngineContractsLocalFeed=false", bootstrap, StringComparison.Ordinal);
        Assert.Contains("log_bootstrap_identity", bootstrap, StringComparison.Ordinal);
        Assert.Contains("bootstrap source:", bootstrap, StringComparison.Ordinal);
        Assert.Contains("to_lower_ascii()", bootstrap, StringComparison.Ordinal);
        Assert.DoesNotContain("${release_proof_status,,}", bootstrap, StringComparison.Ordinal);
        Assert.DoesNotContain("${ui_gate_status,,}", bootstrap, StringComparison.Ordinal);
        Assert.Contains("bootstrap template not found", wrapper, StringComparison.Ordinal);
        Assert.Contains("downloads/release-upload", wrapper, StringComparison.Ordinal);
        Assert.Contains("artifacts/mac-codex-release-pipeline/bootstrap.sh", wrapper, StringComparison.Ordinal);
        Assert.DoesNotContain("/docker/chummercomplete/chummer.run-services/Chummer.Run.Api/wwwroot/artifacts/mac-codex-release-pipeline/bootstrap.sh", wrapper, StringComparison.Ordinal);
        Assert.Contains("downloads/release-upload", maintenanceReadme, StringComparison.Ordinal);
        Assert.Contains("run-mac-release-bootstrap.sh", maintenanceReadme, StringComparison.Ordinal);
        Assert.DoesNotContain("/docker/chummercomplete/chummer.run-services/Chummer.Run.Api/wwwroot/artifacts/mac-codex-release-pipeline/bootstrap.sh", maintenanceReadme, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_MAC_RELEASE_TMPDIR", selfHostedRunbook, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_DESKTOP_INSTALLER_TMPDIR", selfHostedRunbook, StringComparison.Ordinal);
        Assert.Contains("hdiutil: create failed - No space left on device", selfHostedRunbook, StringComparison.Ordinal);
        Assert.Contains("downloads/release-upload", publicReadme, StringComparison.Ordinal);
        Assert.Contains("run-mac-release-bootstrap.sh", publicReadme, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_MAC_RELEASE_MIN_FREE_GIB", publicReadme, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_MAC_RELEASE_TMPDIR", publicReadme, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_DESKTOP_INSTALLER_TMPDIR", publicReadme, StringComparison.Ordinal);
    }

    [Fact]
    public void MacDownloadsUseBootstrapScriptHandoffInsteadOfRawDmgDefault()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string downloadsControllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "DownloadsCompatibilityController.cs");
        string servicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "ReleaseSelectionService.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string downloadsViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Downloads.cshtml");
        string dispatchViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "DownloadDispatch.cshtml");

        string controller = File.ReadAllText(controllerPath);
        string downloadsController = File.ReadAllText(downloadsControllerPath);
        string service = File.ReadAllText(servicePath);
        string viewModel = File.ReadAllText(viewModelPath);
        string downloadsView = File.ReadAllText(downloadsViewPath);
        string dispatchView = File.ReadAllText(dispatchViewPath);

        Assert.Contains("/downloads/install/{artifactId}/bootstrap.command", controller, StringComparison.Ordinal);
        Assert.Contains("/downloads/install/{artifactId}/bootstrap.ps1", controller, StringComparison.Ordinal);
        Assert.Contains("/downloads/install/{artifactId}/bootstrap.sh", controller, StringComparison.Ordinal);
        Assert.Contains("RenderMacInstallBootstrapScript", controller, StringComparison.Ordinal);
        Assert.Contains("RenderWindowsInstallBootstrapScript", controller, StringComparison.Ordinal);
        Assert.Contains("RenderLinuxInstallBootstrapScript", controller, StringComparison.Ordinal);
        Assert.Contains("asks which Chummer apps to install and where to put them", controller, StringComparison.Ordinal);
        Assert.Contains("The Mac setup assistant offers Auto select", controller, StringComparison.Ordinal);
        Assert.Contains("The Windows setup assistant offers Auto select", controller, StringComparison.Ordinal);
        Assert.Contains("The Linux setup assistant offers Auto select", controller, StringComparison.Ordinal);
        Assert.Contains("whether to leave quick access in Applications only or add Desktop links", controller, StringComparison.Ordinal);
        Assert.Contains("staged account linking for first open", controller, StringComparison.Ordinal);
        Assert.Contains("verify_download_digest", controller, StringComparison.Ordinal);
        Assert.Contains("perform_staged_install", controller, StringComparison.Ordinal);
        Assert.Contains("launch_bundle_binary_with_claim", controller, StringComparison.Ordinal);
        Assert.Contains("ConvertFrom-Json", controller, StringComparison.Ordinal);
        Assert.Contains("--bootstrap-install", controller, StringComparison.Ordinal);
        Assert.Contains("--install-claim-code", controller, StringComparison.Ordinal);
        Assert.Contains("dpkg-deb -x", controller, StringComparison.Ordinal);
        Assert.Contains("resolve_install_state_root", controller, StringComparison.Ordinal);
        Assert.Contains("build_install_state_path", controller, StringComparison.Ordinal);
        Assert.Contains("build_pending_claim_code_path", controller, StringComparison.Ordinal);
        Assert.Contains("persist_pending_claim_code", controller, StringComparison.Ordinal);
        Assert.Contains("Prepared first-open account linking", controller, StringComparison.Ordinal);
        Assert.Contains("create_desktop_link", controller, StringComparison.Ordinal);
        Assert.Contains("run_privileged_script", controller, StringComparison.Ordinal);
        Assert.Contains("Current Mac architecture", controller, StringComparison.Ordinal);
        Assert.Contains("open -n \\\"$target_app\\\" >/dev/null 2>&1 || true", controller, StringComparison.Ordinal);
        Assert.Contains("kill \\\"$launch_pid\\\"", controller, StringComparison.Ordinal);
        Assert.Contains("wait \\\"$launch_pid\\\" >/dev/null 2>&1 || true", controller, StringComparison.Ordinal);
        Assert.DoesNotContain("copies them to your clipboard", controller, StringComparison.Ordinal);
        Assert.DoesNotContain("xattr -dr com.apple.quarantine", controller, StringComparison.Ordinal);
        Assert.DoesNotContain("pkill -f \"$target_app/Contents/MacOS\"", controller, StringComparison.Ordinal);
        Assert.Contains("InstallBootstrapTicketService", controller, StringComparison.Ordinal);
        Assert.Contains("QueryString.Create(\"ticket\"", controller, StringComparison.Ordinal);
        Assert.Contains("invalid_or_expired_install_ticket", controller, StringComparison.Ordinal);
        Assert.Contains("claimCode", controller, StringComparison.Ordinal);
        Assert.Contains("invalid_or_expired_claim_code", downloadsController, StringComparison.Ordinal);
        Assert.Contains("UsesMacBootstrapScript", service, StringComparison.Ordinal);
        Assert.Contains("visible_as_account_gated_setup_script_preview", service, StringComparison.Ordinal);
        Assert.Contains("DetectPreferredArchitecture", service, StringComparison.Ordinal);
        Assert.Contains("one Terminal command", service, StringComparison.Ordinal);
        Assert.Contains("verifies the published DMG digest", service, StringComparison.Ordinal);
        Assert.Contains("macOS (Apple Silicon)", service, StringComparison.Ordinal);
        Assert.Contains("BootstrapScriptDownload", viewModel, StringComparison.Ordinal);
        Assert.Contains("TerminalInstallCommand", viewModel, StringComparison.Ordinal);
        Assert.Contains("BootstrapCommandLabel", viewModel, StringComparison.Ordinal);
        Assert.Contains("BootstrapCommandIntro", viewModel, StringComparison.Ordinal);
        Assert.Contains("BootstrapCommandNote", viewModel, StringComparison.Ordinal);
        Assert.Contains("CopyCommandLabel", viewModel, StringComparison.Ordinal);
        Assert.Contains("CompactDispatchLayout", viewModel, StringComparison.Ordinal);
        Assert.Contains("BootstrapFeatureCards", viewModel, StringComparison.Ordinal);
        Assert.Contains("CurrentReleaseSummary", viewModel, StringComparison.Ordinal);
        Assert.Contains("AutoStartDownload", viewModel, StringComparison.Ordinal);
        Assert.Contains("ClaimExchangeUrl", viewModel, StringComparison.Ordinal);
        Assert.DoesNotContain("downloads-quicknav", downloadsView, StringComparison.Ordinal);
        Assert.Contains("Advanced download options", downloadsView, StringComparison.Ordinal);
        Assert.Contains("copy the Terminal command", downloadsView, StringComparison.Ordinal);
        Assert.Contains("BuildMacBootstrapTerminalCommand", controller, StringComparison.Ordinal);
        Assert.Contains("ResolveClaimTicketForDownload", controller, StringComparison.Ordinal);
        Assert.Contains("CopyCommandLabel", controller, StringComparison.Ordinal);
        Assert.Contains("BootstrapCommandIntro", dispatchView, StringComparison.Ordinal);
        Assert.Contains("BootstrapCommandLabel", dispatchView, StringComparison.Ordinal);
        Assert.Contains("CopyCommandLabel", dispatchView, StringComparison.Ordinal);
        Assert.Contains("CompactDispatchLayout", dispatchView, StringComparison.Ordinal);
        Assert.Contains("BootstrapFeatureCards", dispatchView, StringComparison.Ordinal);
        Assert.Contains("CurrentReleaseSummary", dispatchView, StringComparison.Ordinal);
        Assert.Contains("Fallbacks and recovery", dispatchView, StringComparison.Ordinal);
        Assert.Contains("Setup highlights", dispatchView, StringComparison.Ordinal);
        Assert.Contains("dispatch-inline-info", dispatchView, StringComparison.Ordinal);
        Assert.Contains("data-install-info", dispatchView, StringComparison.Ordinal);
        Assert.Contains("Install details", dispatchView, StringComparison.Ordinal);
        Assert.Contains("enter the support or recovery code inside setup instead of copying one from this page", dispatchView, StringComparison.Ordinal);
        Assert.Contains("claimExchangeUrl", dispatchView, StringComparison.Ordinal);
        Assert.DoesNotContain("_PublicTrustPulsePanel.cshtml", dispatchView, StringComparison.Ordinal);
        Assert.Contains("if (autoStartDownload)", dispatchView, StringComparison.Ordinal);
        Assert.Contains("SecondaryDownloadLabel", dispatchView, StringComparison.Ordinal);
        Assert.Contains("RebindDownloadsHeaderActions", controller, StringComparison.Ordinal);
        Assert.Contains("GuestGateSecondaryHref", controller, StringComparison.Ordinal);
        Assert.Contains("GuestGatePrimaryHref", controller, StringComparison.Ordinal);
    }

    [Fact]
    public void MacReleaseBootstrapSupportsCleanStockMacLayouts()
    {
        string bootstrapPath = RepoPaths.FromRoot("Chummer.Run.Api", "wwwroot", "artifacts", "mac-codex-release-pipeline", "bootstrap.sh");
        string bootstrap = File.ReadAllText(bootstrapPath);

        Assert.Contains("ensure_dotnet_resolver", bootstrap, StringComparison.Ordinal);
        Assert.Contains("$HOME/.dotnet", bootstrap, StringComparison.Ordinal);
        Assert.Contains("dotnet --list-sdks", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_LOCAL_CAMPAIGN_CONTRACTS_PROJECT", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_LOCAL_HUB_REGISTRY_CONTRACTS_PROJECT", bootstrap, StringComparison.Ordinal);
        Assert.Contains("chummer6-media-factory.git", bootstrap, StringComparison.Ordinal);
        Assert.Contains("validate_publish_mode", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_RELEASE_UPLOAD_TOKEN", bootstrap, StringComparison.Ordinal);
        Assert.Contains("[[ \"${#app_heads[@]}\" -eq 0 ]]", bootstrap, StringComparison.Ordinal);
        Assert.Contains("upload_release_bundle_http()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("write_public_promotion_evidence()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("require_min_free_space_gib", bootstrap, StringComparison.Ordinal);
        Assert.Contains("log_disk_space", bootstrap, StringComparison.Ordinal);
        Assert.Contains("hdiutil create for signed repack", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_MAC_RELEASE_MIN_FREE_GIB", bootstrap, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_MAC_RELEASE_TMPDIR", bootstrap, StringComparison.Ordinal);
        Assert.Contains("export TMPDIR=\"$temp_root\"", bootstrap, StringComparison.Ordinal);
        Assert.Contains("export CHUMMER_DESKTOP_INSTALLER_TMPDIR=\"$TMPDIR/desktop-installer\"", bootstrap, StringComparison.Ordinal);
        Assert.Contains("temporary packaging root: $TMPDIR", bootstrap, StringComparison.Ordinal);
        Assert.Contains("desktop installer temp root: $CHUMMER_DESKTOP_INSTALLER_TMPDIR", bootstrap, StringComparison.Ordinal);
        Assert.Contains("json_generated_at_health()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("generate_hub_local_release_proof()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("generate_ui_localization_release_gate()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("resolve_ui_localization_release_gate_repo()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("allow_remote_release_proof_inputs()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("official_chummer_release_proof_url()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("allow_remote_release_proof_input_for_candidate()", bootstrap, StringComparison.Ordinal);
        Assert.Contains("ui localization release gate generator is using fallback repo", bootstrap, StringComparison.Ordinal);
        Assert.Contains("/docker/chummercomplete/chummer-presentation", bootstrap, StringComparison.Ordinal);
        Assert.Contains("https://chummer.run/proofs/mac-codex-release/HUB_LOCAL_RELEASE_PROOF.generated.json", bootstrap, StringComparison.Ordinal);
        Assert.Contains("https://chummer.run/proofs/mac-codex-release/UI_LOCALIZATION_RELEASE_GATE.generated.json", bootstrap, StringComparison.Ordinal);
        Assert.Contains("Ignoring remote requested release proof because remote proof inputs are disabled by default for non-official endpoints", bootstrap, StringComparison.Ordinal);
        Assert.Contains("Ignoring remote requested release gate because remote proof inputs are disabled by default for non-official endpoints", bootstrap, StringComparison.Ordinal);
        Assert.Contains("Cache-Control: no-cache", bootstrap, StringComparison.Ordinal);
        Assert.Contains("release proof validation failed before build", bootstrap, StringComparison.Ordinal);
        Assert.Contains("bootstrap synthetic hub local release-proof fallback is disabled", bootstrap, StringComparison.Ordinal);
        Assert.Contains("regenerated release proof validated against the current ui localization release gate", bootstrap, StringComparison.Ordinal);
        Assert.Contains("retrying regenerated release proof against a freshly generated ui localization release gate", bootstrap, StringComparison.Ordinal);
        Assert.Contains("generated fresh ui localization release gate", bootstrap, StringComparison.Ordinal);
        Assert.Contains("release proof", bootstrap, StringComparison.Ordinal);
        Assert.Contains("ui localization release gate", bootstrap, StringComparison.Ordinal);
        Assert.DoesNotContain("companion_runtime", bootstrap, StringComparison.Ordinal);
        Assert.DoesNotContain("scripts/generate-public-promotion-evidence.py", bootstrap, StringComparison.Ordinal);
        Assert.DoesNotContain("bash scripts/publish-download-bundle-http.sh", bootstrap, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicEdgeComposePinsHttpsPortForLocalChummerRunRedirects()
    {
        string composePath = RepoPaths.FromRoot("docker-compose.public-edge.yml");
        string compose = File.ReadAllText(composePath);

        Assert.Contains("ASPNETCORE_HTTPS_PORT", compose, StringComparison.Ordinal);
        Assert.Contains("${ASPNETCORE_HTTPS_PORT:-443}", compose, StringComparison.Ordinal);
    }

    [Fact]
    public void HubLiveAuditSupportsReverseProxiedLocalEdgeMode()
    {
        string auditPath = RepoPaths.FromRoot("scripts", "hub-live-audit.py");
        string audit = File.ReadAllText(auditPath);

        Assert.Contains("--public-host", audit, StringComparison.Ordinal);
        Assert.Contains("--forwarded-proto", audit, StringComparison.Ordinal);
        Assert.Contains("--verify-http-redirects", audit, StringComparison.Ordinal);
        Assert.Contains("X-Forwarded-Proto", audit, StringComparison.Ordinal);
        Assert.Contains("Accept-Language", audit, StringComparison.Ordinal);
        Assert.Contains("Cache-Control", audit, StringComparison.Ordinal);
        Assert.Contains("Pragma", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasonops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasonop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=season-operation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=season-operations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasoncontrol", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=season%20control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasoncontrols", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasonctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasonctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=seasonctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=season%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrol", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrols", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event%20control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event-control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event-op", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event-operation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=event-operations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-op", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20operation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-operation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20operations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-operations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrol", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrols", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasterctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasterctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-ctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20ctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-controls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gm-ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leagueops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leagueop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-op", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leagueoperation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-operation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leagueoperations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-operations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20op", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leaguecontrol", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leaguecontrols", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leaguectrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leaguectl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leaguectls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=leaguectrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20ctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-ctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=league-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-op", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityoperation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-operation", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityoperations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-operations", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20op", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-ops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communitycontrol", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communitycontrols", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-control", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=communityctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20ctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-ctl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-ctls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=community-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heat", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heats", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=encounter", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=enemy", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hostile", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=adversary", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=threat", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=opfor", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=opforce", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=opforces", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=opfors", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=op-force", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=op%20force", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contact", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contacts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connection", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connections", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationship", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationships", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactchanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactschanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactchange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactschange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionchanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionsupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionsupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionschanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionchange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=connectionschange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipchanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipsupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipsupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipschanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipchange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=relationshipschange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=faction", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=factions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatchanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatchange", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=journal", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=journals", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionlog", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionlogs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session%20logs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionlogupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionlogupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionlogchanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diary", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaries", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryupdate", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryupdates", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diarychanged", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtime", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermath", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermaths", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=debrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=debriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=debriefed", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=debriefing", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=debriefings", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=outbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=outbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=outbriefed", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=outbriefing", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=outbriefings", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out%20brief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out%20briefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out%20briefed", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out%20briefing", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out%20briefings", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out-brief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out-briefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out-briefed", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out-briefing", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=out-briefings", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postmortem", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20mortem", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-mortem", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postmortems", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20mortems", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-mortems", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postsession", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20session", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-session", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postsessions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20sessions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-sessions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postrun", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20run", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-run", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postruns", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20runs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-runs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postgame", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=postgames", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20game", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post%20games", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-game", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=post-games", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=recap", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=recaps", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=afteraction", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=afteractions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=afteractionreport", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=afteractionreports", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=afteractionreview", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=afteractionreviews", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after%20action", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after%20actions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after%20action%20report", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after%20action%20reports", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after%20action%20review", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after%20action%20reviews", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after-action", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after-actions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after-action%20report", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after-action%20reports", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after-action%20review", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=after-action%20reviews", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aar", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aars", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=retro", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=retros", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=retrospective", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=retrospectives", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hotwash", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hotwashes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hot%20wash", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hot%20washes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hot-wash", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=hot-washes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessonlearned", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessonslearned", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessonlearnt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessonslearnt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lesson%20learned", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessons%20learned", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lesson%20learnt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessons%20learnt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lesson-learned", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessons-learned", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lesson-learnt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=lessons-learnt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returns", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturn", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturn", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturn", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturn", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarypacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarypackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarypkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarypkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarybrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarybriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarybrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplibrarybrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=oppositionsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermovementbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostersmovementbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventcontrolsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoppacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoppackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoppkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoppkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventopsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=eventoperationsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimereturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=downtimesreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diariesreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diaryreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=contactreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=heatreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diarycontactheatpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diarycontactheatpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diarycontactsheatpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=diarycontactsheatpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathdowntimepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathdowntimepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsdowntimepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=aftermathsdowntimepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelsofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travel%20offline%20packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travel%20offline%20packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travel-offline-packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travel-offline-packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile%20offline%20packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile%20offline%20packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile-offline-packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile-offline-packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousetravelbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousestravelbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse%20travel%20packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse%20travel%20packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse-travel-packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse-travel-packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouseofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousesofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse%20offline%20packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse%20offline%20packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse-offline-packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehouse-offline-packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehouseofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinepacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinepackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinepkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinepkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinebrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinebriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinebrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousesofflinebrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile%20safehouse%20offline%20packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile%20safehouse%20offline%20packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile-safehouse-offline-packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobile-safehouse-offline-packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmopsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmoperationsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gmcontrolsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolspacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolspackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolspkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolspkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteropsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemasteroperationsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolsbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolsbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolsbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=gamemastercontrolsbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=workspacev4", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignworkspacev4", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=workspacev4packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=workspacev4packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=workspacev4brief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=workspacev4briefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignworkspacev4packet", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignworkspacev4packets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignworkspacev4brief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignworkspacev4briefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return%20lane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return%20lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return-lane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return-lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsession", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessions", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturn", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturns", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=nextsessionloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturn", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturns", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=sessionreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session%20return%20loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session-return-loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session%20return%20lane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session%20return%20lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session-return-lane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=session-return-lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return%20loop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return%20loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=return-loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next%20session%20return%20lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next-session-return-lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next%20session%20return%20loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next-session-return-loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next%20sessions%20return%20lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next-sessions-return-lanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next%20sessions%20return%20loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=next-sessions-return-loops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=memory", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=memories", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=archive", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=archives", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=history", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=histories", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=timeline", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=timelines", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=ledger", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=ledgers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermove", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostermoves", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostershift", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostershifts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewmove", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewmoves", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewshift", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewshifts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewswap", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewswaps", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rosterswap", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rosterswaps", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostertransfer", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rostertransfers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rosterhandoff", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rosterhandoffs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rosterhandover", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=rosterhandovers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewhandoff", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewhandoffs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewtransfer", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crewtransfers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew%20transfer", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew%20transfers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew%20handoff", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew%20handoffs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew%20move", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew%20moves", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20transfer", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20transfers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20handoff", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20handoffs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20handover", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20handovers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20move", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster%20moves", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster-move", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew-move", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster-transfer", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew-transfer", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster-handoff", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster-handover", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=roster-handovers", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=crew-handoff", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplaunch", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=preplaunches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelprefetch", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelprefetches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=offlinereadiness", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=offlinereadinesses", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelreadiness", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelreadinesses", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousereadiness", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousereadinesses", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=offlinecache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=offlinecaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=stalecache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=stalecaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=staleofflinecache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=staleofflinecaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelcache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=travelcaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousecache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=safehousecaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinereadiness", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinereadinesses", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobiletravelreadiness", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobiletravelreadinesses", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinecache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobileofflinecaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobiletravelcache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobiletravelcaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousereadiness", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousereadinesses", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousecache", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilesafehousecaches", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=mobilecompanionsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnloop", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnpacket", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnpackets", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnpkt", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnpkts", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnbrief", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionreturnbrf", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=campaignmobilecompanionsreturnbrfs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasonops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasonop", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=season-operation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=season-operations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasoncontrol", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=season%20control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasoncontrols", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasonctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasonctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=seasonctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=season%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventcontrol", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventcontrols", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event%20control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event-control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventop", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventoperation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=eventoperations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event-op", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event-operation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=event-operations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmop", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-op", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmoperation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmoperations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20operation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-operation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20operations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-operations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmcontrol", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmcontrols", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gmctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gamemasterctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gamemasterctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-ctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20ctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-controls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=gm-ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarypacket\", \"preplibrarypacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarypackets\", \"preplibrarypackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarypkt\", \"preplibrarypkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarypkts\", \"preplibrarypkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarybrief\", \"preplibrarybrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarybriefs\", \"preplibrarybriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarybrf\", \"preplibrarybrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"preplibrarybrfs\", \"preplibrarybrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionpacket\", \"oppositionpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionpackets\", \"oppositionpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionpkt\", \"oppositionpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionpkts\", \"oppositionpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionspacket\", \"oppositionspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionspackets\", \"oppositionspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionspkt\", \"oppositionspkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionspkts\", \"oppositionspkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovepacket\", \"rostermovepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovepackets\", \"rostermovepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovepkt\", \"rostermovepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovepkts\", \"rostermovepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementpacket\", \"rostermovementpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementpackets\", \"rostermovementpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementpkt\", \"rostermovementpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementpkts\", \"rostermovementpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovepacket\", \"rostersmovepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovepackets\", \"rostersmovepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovepkt\", \"rostersmovepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovepkts\", \"rostersmovepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementpacket\", \"rostersmovementpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementpackets\", \"rostersmovementpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementpkt\", \"rostersmovementpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementpkts\", \"rostersmovementpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolpacket\", \"eventcontrolpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolpackets\", \"eventcontrolpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolpkt\", \"eventcontrolpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolpkts\", \"eventcontrolpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnpacket\", \"campaignreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnpackets\", \"campaignreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnpkt\", \"campaignreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnpkts\", \"campaignreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnbrief\", \"campaignreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnbriefs\", \"campaignreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnbrf\", \"campaignreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignreturnbrfs\", \"campaignreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnloop\", \"campaignsreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnpacket\", \"campaignsreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnpackets\", \"campaignsreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnpkt\", \"campaignsreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnpkts\", \"campaignsreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnbrief\", \"campaignsreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnbriefs\", \"campaignsreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnbrf\", \"campaignsreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignsreturnbrfs\", \"campaignsreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnpacket\", \"aftermathreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnpackets\", \"aftermathreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnpkt\", \"aftermathreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnpkts\", \"aftermathreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnbrief\", \"aftermathreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnbriefs\", \"aftermathreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnbrf\", \"aftermathreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathreturnbrfs\", \"aftermathreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnpacket\", \"aftermathsreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnpackets\", \"aftermathsreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnpkt\", \"aftermathsreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnpkts\", \"aftermathsreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnbrief\", \"aftermathsreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnbriefs\", \"aftermathsreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnbrf\", \"aftermathsreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsreturnbrfs\", \"aftermathsreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnpacket\", \"downtimereturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnpackets\", \"downtimereturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnpkt\", \"downtimereturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnpkts\", \"downtimereturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnbrief\", \"downtimereturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnbriefs\", \"downtimereturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnbrf\", \"downtimereturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimereturnbrfs\", \"downtimereturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnloop\", \"downtimesreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnpacket\", \"downtimesreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnpackets\", \"downtimesreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnpkt\", \"downtimesreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnpkts\", \"downtimesreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnbrief\", \"downtimesreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnbriefs\", \"downtimesreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnbrf\", \"downtimesreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"downtimesreturnbrfs\", \"downtimesreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnloop\", \"diariesreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnloop\", \"diaryreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnloops\", \"diaryreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnlane\", \"diaryreturnlane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnlanes\", \"diaryreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnpacket\", \"diariesreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnpackets\", \"diariesreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnpkt\", \"diariesreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnpkts\", \"diariesreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnbrief\", \"diariesreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnbriefs\", \"diariesreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnbrf\", \"diariesreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diariesreturnbrfs\", \"diariesreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnpacket\", \"diaryreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnpackets\", \"diaryreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnpkt\", \"diaryreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnpkts\", \"diaryreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnbrief\", \"diaryreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnbriefs\", \"diaryreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnbrf\", \"diaryreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diaryreturnbrfs\", \"diaryreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnloop\", \"contactsreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnloop\", \"contactreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnloops\", \"contactreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnlane\", \"contactreturnlane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnlanes\", \"contactreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnpacket\", \"contactsreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnpackets\", \"contactsreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnpkt\", \"contactsreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnpkts\", \"contactsreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnbrief\", \"contactsreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnbriefs\", \"contactsreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnbrf\", \"contactsreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactsreturnbrfs\", \"contactsreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnpacket\", \"contactreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnpackets\", \"contactreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnpkt\", \"contactreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnpkts\", \"contactreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnbrief\", \"contactreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnbriefs\", \"contactreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnbrf\", \"contactreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"contactreturnbrfs\", \"contactreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnloop\", \"heatsreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnloop\", \"heatreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnloops\", \"heatreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnlane\", \"heatreturnlane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnlanes\", \"heatreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnpacket\", \"heatsreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnpackets\", \"heatsreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnpkt\", \"heatsreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnpkts\", \"heatsreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnbrief\", \"heatsreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnbriefs\", \"heatsreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnbrf\", \"heatsreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatsreturnbrfs\", \"heatsreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnpacket\", \"heatreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnpackets\", \"heatreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnpkt\", \"heatreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnpkts\", \"heatreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnbrief\", \"heatreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnbriefs\", \"heatreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnbrf\", \"heatreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"heatreturnbrfs\", \"heatreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diarycontactheatpacket\", \"diarycontactheatpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diarycontactheatpackets\", \"diarycontactheatpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diarycontactsheatpacket\", \"diarycontactsheatpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"diarycontactsheatpackets\", \"diarycontactsheatpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathdowntimepacket\", \"aftermathdowntimepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathdowntimepackets\", \"aftermathdowntimepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsdowntimepacket\", \"aftermathsdowntimepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"aftermathsdowntimepackets\", \"aftermathsdowntimepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinepacket\", \"travelofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinepackets\", \"travelofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinepkt\", \"travelofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinepkts\", \"travelofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinebrief\", \"travelofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinebriefs\", \"travelofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinebrf\", \"travelofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelofflinebrfs\", \"travelofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinepacket\", \"travelsofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinepackets\", \"travelsofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinepkt\", \"travelsofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinepkts\", \"travelsofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinebrief\", \"travelsofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinebriefs\", \"travelsofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinebrf\", \"travelsofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travelsofflinebrfs\", \"travelsofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travel%20offline%20packet\", \"travel offline packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travel%20offline%20packets\", \"travel offline packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travel-offline-packet\", \"travel-offline-packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"travel-offline-packets\", \"travel-offline-packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinepacket\", \"mobileofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinepackets\", \"mobileofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinepkt\", \"mobileofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinepkts\", \"mobileofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinebrief\", \"mobileofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinebriefs\", \"mobileofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinebrf\", \"mobileofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobileofflinebrfs\", \"mobileofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile%20offline%20packet\", \"mobile offline packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile%20offline%20packets\", \"mobile offline packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile-offline-packet\", \"mobile-offline-packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile-offline-packets\", \"mobile-offline-packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelpacket\", \"safehousetravelpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelpackets\", \"safehousetravelpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelpkt\", \"safehousetravelpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelpkts\", \"safehousetravelpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelbrief\", \"safehousetravelbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelbriefs\", \"safehousetravelbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelbrf\", \"safehousetravelbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousetravelbrfs\", \"safehousetravelbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelpacket\", \"safehousestravelpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelpackets\", \"safehousestravelpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelpkt\", \"safehousestravelpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelpkts\", \"safehousestravelpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelbrief\", \"safehousestravelbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelbriefs\", \"safehousestravelbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelbrf\", \"safehousestravelbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousestravelbrfs\", \"safehousestravelbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse%20travel%20packet\", \"safehouse travel packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse%20travel%20packets\", \"safehouse travel packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse-travel-packet\", \"safehouse-travel-packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse-travel-packets\", \"safehouse-travel-packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinepacket\", \"safehouseofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinepackets\", \"safehouseofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinepkt\", \"safehouseofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinepkts\", \"safehouseofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinebrief\", \"safehouseofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinebriefs\", \"safehouseofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinebrf\", \"safehouseofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouseofflinebrfs\", \"safehouseofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinepacket\", \"safehousesofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinepackets\", \"safehousesofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinepkt\", \"safehousesofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinepkts\", \"safehousesofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinebrief\", \"safehousesofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinebriefs\", \"safehousesofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinebrf\", \"safehousesofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehousesofflinebrfs\", \"safehousesofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse%20offline%20packet\", \"safehouse offline packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse%20offline%20packets\", \"safehouse offline packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse-offline-packet\", \"safehouse-offline-packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"safehouse-offline-packets\", \"safehouse-offline-packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinepacket\", \"mobilesafehouseofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinepackets\", \"mobilesafehouseofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinepkt\", \"mobilesafehouseofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinepkts\", \"mobilesafehouseofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinebrief\", \"mobilesafehouseofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinebriefs\", \"mobilesafehouseofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinebrf\", \"mobilesafehouseofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehouseofflinebrfs\", \"mobilesafehouseofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinepacket\", \"mobilesafehousesofflinepacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinepackets\", \"mobilesafehousesofflinepackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinepkt\", \"mobilesafehousesofflinepkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinepkts\", \"mobilesafehousesofflinepkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinebrief\", \"mobilesafehousesofflinebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinebriefs\", \"mobilesafehousesofflinebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinebrf\", \"mobilesafehousesofflinebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilesafehousesofflinebrfs\", \"mobilesafehousesofflinebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile%20safehouse%20offline%20packet\", \"mobile safehouse offline packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile%20safehouse%20offline%20packets\", \"mobile safehouse offline packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile-safehouse-offline-packet\", \"mobile-safehouse-offline-packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobile-safehouse-offline-packets\", \"mobile-safehouse-offline-packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmopspacket\", \"gmopspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmopspackets\", \"gmopspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationpacket\", \"gmoperationpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationpackets\", \"gmoperationpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationspacket\", \"gmoperationspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationspackets\", \"gmoperationspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolpacket\", \"gmcontrolpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolpackets\", \"gmcontrolpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolspacket\", \"gmcontrolspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolspackets\", \"gmcontrolspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmopsbrief\", \"gmopsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmopsbriefs\", \"gmopsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationbrief\", \"gmoperationbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationbriefs\", \"gmoperationbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationsbrief\", \"gmoperationsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmoperationsbriefs\", \"gmoperationsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolbrief\", \"gmcontrolbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolbriefs\", \"gmcontrolbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolsbrief\", \"gmcontrolsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gmcontrolsbriefs\", \"gmcontrolsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionbrief\", \"oppositionbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionbriefs\", \"oppositionbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionbrf\", \"oppositionbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionbrfs\", \"oppositionbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionsbrief\", \"oppositionsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionsbriefs\", \"oppositionsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionsbrf\", \"oppositionsbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"oppositionsbrfs\", \"oppositionsbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovebrief\", \"rostermovebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovebriefs\", \"rostermovebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementbrief\", \"rostermovementbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementbriefs\", \"rostermovementbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovebrf\", \"rostermovebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovebrfs\", \"rostermovebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementbrf\", \"rostermovementbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostermovementbrfs\", \"rostermovementbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovebrief\", \"rostersmovebrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovebriefs\", \"rostersmovebriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementbrief\", \"rostersmovementbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementbriefs\", \"rostersmovementbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovebrf\", \"rostersmovebrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovebrfs\", \"rostersmovebrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementbrf\", \"rostersmovementbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"rostersmovementbrfs\", \"rostersmovementbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolbrief\", \"eventcontrolbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolbriefs\", \"eventcontrolbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolbrf\", \"eventcontrolbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolbrfs\", \"eventcontrolbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolspacket\", \"eventcontrolspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolspackets\", \"eventcontrolspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolspkt\", \"eventcontrolspkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolspkts\", \"eventcontrolspkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolsbrief\", \"eventcontrolsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolsbriefs\", \"eventcontrolsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolsbrf\", \"eventcontrolsbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"eventcontrolsbrfs\", \"eventcontrolsbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteropspacket\", \"gamemasteropspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteropspackets\", \"gamemasteropspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationpacket\", \"gamemasteroperationpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationpackets\", \"gamemasteroperationpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationspacket\", \"gamemasteroperationspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationspackets\", \"gamemasteroperationspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolpacket\", \"gamemastercontrolpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolpackets\", \"gamemastercontrolpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolspacket\", \"gamemastercontrolspacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolspackets\", \"gamemastercontrolspackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteropsbrief\", \"gamemasteropsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteropsbriefs\", \"gamemasteropsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationbrief\", \"gamemasteroperationbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationbriefs\", \"gamemasteroperationbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationsbrief\", \"gamemasteroperationsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemasteroperationsbriefs\", \"gamemasteroperationsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolbrief\", \"gamemastercontrolbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolbriefs\", \"gamemastercontrolbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolsbrief\", \"gamemastercontrolsbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"gamemastercontrolsbriefs\", \"gamemastercontrolsbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"workspacev4\", \"workspacev4\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignworkspacev4\", \"campaignworkspacev4\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"workspacev4packet\", \"workspacev4packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"workspacev4packets\", \"workspacev4packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"workspacev4brief\", \"workspacev4brief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"workspacev4briefs\", \"workspacev4briefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignworkspacev4packet\", \"campaignworkspacev4packet\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignworkspacev4packets\", \"campaignworkspacev4packets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignworkspacev4brief\", \"campaignworkspacev4brief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignworkspacev4briefs\", \"campaignworkspacev4briefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionreturnlanes\", \"mobilecompanionreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionsreturnloop\", \"mobilecompanionsreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionsreturnloops\", \"mobilecompanionsreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionreturnpacket\", \"mobilecompanionreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionsreturnpackets\", \"mobilecompanionsreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionreturnpkt\", \"mobilecompanionreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionsreturnpkts\", \"mobilecompanionsreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionreturnbrief\", \"mobilecompanionreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionsreturnbriefs\", \"mobilecompanionsreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionreturnbrf\", \"mobilecompanionreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"mobilecompanionsreturnbrfs\", \"mobilecompanionsreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionreturnlane\", \"campaignmobilecompanionreturnlane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionreturnlanes\", \"campaignmobilecompanionreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionsreturnloop\", \"campaignmobilecompanionsreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionsreturnloops\", \"campaignmobilecompanionsreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionreturnpacket\", \"campaignmobilecompanionreturnpacket\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionsreturnpackets\", \"campaignmobilecompanionsreturnpackets\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionreturnpkt\", \"campaignmobilecompanionreturnpkt\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionsreturnpkts\", \"campaignmobilecompanionsreturnpkts\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionreturnbrief\", \"campaignmobilecompanionreturnbrief\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionsreturnbriefs\", \"campaignmobilecompanionsreturnbriefs\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionreturnbrf\", \"campaignmobilecompanionreturnbrf\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"campaignmobilecompanionsreturnbrfs\", \"campaignmobilecompanionsreturnbrfs\")", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=heat", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=heats", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=oppositions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=encounter", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=enemy", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hostile", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=adversary", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=threat", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=opfor", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=opforce", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=opforces", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=opfors", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=op-force", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=op%20force", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=contact", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=contacts", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=connection", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=connections", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=relationship", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=relationships", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=faction", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=factions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=journal", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=journals", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=sessionlog", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=sessionlogs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=session%20logs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=diary", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=diaries", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=downtime", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=downtimes", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=aftermath", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=aftermaths", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=debrief", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=debriefs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=debriefing", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=debriefings", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=outbrief", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=outbriefs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=outbriefed", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=outbriefing", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=outbriefings", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out%20brief", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out%20briefs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out%20briefed", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out%20briefing", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out%20briefings", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out-brief", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out-briefs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out-briefed", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out-briefing", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=out-briefings", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postmortem", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20mortem", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-mortem", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postmortems", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20mortems", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-mortems", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postsession", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20session", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-session", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postsessions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20sessions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-sessions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postrun", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20run", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-run", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postruns", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20runs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-runs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postgame", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=postgames", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20game", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post%20games", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-game", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=post-games", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=recap", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=recaps", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=afteraction", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=afteractions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=afteractionreport", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=afteractionreports", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=afteractionreview", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=afteractionreviews", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after%20action", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after%20actions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after%20action%20report", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after%20action%20reports", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after%20action%20review", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after%20action%20reviews", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after-action", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after-actions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after-action%20report", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after-action%20reports", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after-action%20review", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=after-action%20reviews", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=aar", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=aars", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=retro", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=retros", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=retrospective", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=retrospectives", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hotwash", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hotwashes", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hot%20wash", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hot%20washes", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hot-wash", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=hot-washes", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessonlearned", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessonslearned", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessonlearnt", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessonslearnt", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lesson%20learned", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessons%20learned", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lesson%20learnt", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessons%20learnt", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lesson-learned", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessons-learned", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lesson-learnt", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=lessons-learnt", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=return", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=returns", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=returnloop", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=returnloops", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returnlane", audit, StringComparison.Ordinal);
        Assert.Contains("queryText=returnlanes", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=nextsession", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=nextsessions", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=nextsessionreturn", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=nextsessionreturns", audit, StringComparison.Ordinal);
        Assert.Contains("(\"nextsessionreturnloop\", \"nextsessionreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"nextsessionreturnloops\", \"nextsessionreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"nextsessionreturnlane\", \"nextsessionreturnlane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"nextsessionreturnlanes\", \"nextsessionreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"nextsessionloop\", \"nextsessionloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"nextsessionloops\", \"nextsessionloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=sessionreturn", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=sessionreturns", audit, StringComparison.Ordinal);
        Assert.Contains("(\"sessionreturnloop\", \"sessionreturnloop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"sessionreturnloops\", \"sessionreturnloops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"sessionreturnlane\", \"sessionreturnlane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"sessionreturnlanes\", \"sessionreturnlanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"session%20return%20loops\", \"session return loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"session-return-loops\", \"session-return-loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"session%20return%20lane\", \"session return lane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"session%20return%20lanes\", \"session return lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"session-return-lane\", \"session-return-lane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"session-return-lanes\", \"session-return-lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return%20loop\", \"return loop\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return%20loops\", \"return loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return-loops\", \"return-loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return%20lane\", \"return lane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return%20lanes\", \"return lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return-lane\", \"return-lane\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"return-lanes\", \"return-lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next%20session%20return%20loops\", \"next session return loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next-session-return-loops\", \"next-session-return-loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next%20session%20return%20lanes\", \"next session return lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next-session-return-lanes\", \"next-session-return-lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next%20sessions%20return%20loops\", \"next sessions return loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next-sessions-return-loops\", \"next-sessions-return-loops\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next%20sessions%20return%20lanes\", \"next sessions return lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("(\"next-sessions-return-lanes\", \"next-sessions-return-lanes\")", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=memory", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=memories", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=archive", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=archives", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=history", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=histories", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=timeline", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=timelines", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=ledger", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=ledgers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rostermove", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rostermoves", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rostershift", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rostershifts", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewmove", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewmoves", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewshift", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewshifts", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewswap", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewswaps", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rosterswap", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rosterswaps", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rostertransfer", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rostertransfers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rosterhandoff", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rosterhandoffs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rosterhandover", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=rosterhandovers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewhandoff", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewhandoffs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewtransfer", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crewtransfers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew%20transfer", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew%20transfers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew%20handoff", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew%20handoffs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew%20move", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew%20moves", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20transfer", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20transfers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20handoff", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20handoffs", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20handover", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20handovers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20move", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster%20moves", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster-move", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew-move", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster-transfer", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew-transfer", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster-handoff", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster-handover", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=roster-handovers", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=crew-handoff", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=preplaunch", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=preplaunches", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=travelprefetch", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=travelprefetches", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leagueops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leagueop", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-op", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leagueoperation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-operation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leagueoperations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-operations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20op", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leaguecontrol", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leaguecontrols", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leaguectrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leaguectl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leaguectls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=leaguectrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20ctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-ctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=league-ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityop", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-op", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityoperation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-operation", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityoperations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-operations", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20op", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-ops", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communitycontrol", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20controls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communitycontrols", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-control", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=communityctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20ctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-ctl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-ctls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-ctrls", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community%20ctrl", audit, StringComparison.Ordinal);
        Assert.Contains("prepQuery=community-ctrl", audit, StringComparison.Ordinal);
    }

    [Fact]
    public void HubCloseoutAndE2EUseReverseProxiedLocalEdgeAudit()
    {
        string closeoutPath = RepoPaths.FromRoot("scripts", "ai", "hub_closeout.sh");
        string e2ePath = RepoPaths.FromRoot("scripts", "e2e-hub.sh");
        string cleanupPath = RepoPaths.FromRoot("scripts", "cleanup_synthetic_support_cases.py");
        string closeout = File.ReadAllText(closeoutPath);
        string e2e = File.ReadAllText(e2ePath);
        string cleanup = File.ReadAllText(cleanupPath);

        Assert.Contains("HUB_PUBLIC_HOST", closeout, StringComparison.Ordinal);
        Assert.Contains("--forwarded-proto https", closeout, StringComparison.Ordinal);
        Assert.Contains("--verify-http-redirects", closeout, StringComparison.Ordinal);
        Assert.Contains("hub-live-audit.py", e2e, StringComparison.Ordinal);
        Assert.Contains("wait_for_hub_edge", e2e, StringComparison.Ordinal);
        Assert.Contains("--public-host \"$HUB_PUBLIC_HOST\"", e2e, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_HUB_PLAYWRIGHT_FORWARDED_PROTO", e2e, StringComparison.Ordinal);
        Assert.Contains("HUB_PUBLIC_HOST", e2e, StringComparison.Ordinal);
        Assert.Contains("--forwarded-proto https", e2e, StringComparison.Ordinal);
        Assert.Contains("--verify-http-redirects", e2e, StringComparison.Ordinal);
        Assert.Contains("--public-host", cleanup, StringComparison.Ordinal);
        Assert.Contains("--forwarded-proto", cleanup, StringComparison.Ordinal);
        Assert.Contains("X-Forwarded-Proto", cleanup, StringComparison.Ordinal);

        string playwrightPath = RepoPaths.FromRoot("scripts", "e2e-hub-playwright.cjs");
        string playwright = File.ReadAllText(playwrightPath);
        Assert.Contains("?prepQuery=seasonops", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasonops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=seasonop", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasonop prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=season-operation", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen season-operation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=season-operations", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen season-operations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=seasoncontrol", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasoncontrol prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=season(?:%20|\\+)control", playwright, StringComparison.Ordinal);
        Assert.Contains("split season control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=seasoncontrols", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasoncontrols prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=seasonctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasonctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=seasonctl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasonctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=seasonctls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact seasonctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=season(?:%20|\\+)ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("split season ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventcontrol", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventcontrol prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventcontrols", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventcontrols prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event(?:%20|\\+)control", playwright, StringComparison.Ordinal);
        Assert.Contains("split event control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event(?:%20|\\+)controls", playwright, StringComparison.Ordinal);
        Assert.Contains("split event controls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event-control", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen event-control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event(?:%20|\\+)ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("split event ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event-ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen event-ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventctl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventctls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event(?:%20|\\+)ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("split event ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventops", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event(?:%20|\\+)ops", playwright, StringComparison.Ordinal);
        Assert.Contains("split event ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventop", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventop prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventoperation", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventoperation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=eventoperations", playwright, StringComparison.Ordinal);
        Assert.Contains("compact eventoperations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event-op", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen event-op prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event-operation", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen event-operation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=event-operations", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen event-operations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmops", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)ops", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-ops", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmop", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmop prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-op", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-op prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmoperation", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmoperation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmoperations", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmoperations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)operation", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm operation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-operation", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-operation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)operations", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm operations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-operations", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-operations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmcontrol", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmcontrol prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmcontrols", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmcontrols prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmctl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gmctls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact gmctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-ctl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-ctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)ctl", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm ctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)ctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm ctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)control", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-control", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)controls", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm controls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-controls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-controls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm(?:%20|\\+)ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("split gm ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=gm-ctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen gm-ctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leagueops", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leagueops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leagueop", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leagueop prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-op", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-op prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leagueoperation", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leagueoperation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-operation", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-operation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leagueoperations", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leagueoperations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-operations", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-operations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)ops", playwright, StringComparison.Ordinal);
        Assert.Contains("split league ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)op", playwright, StringComparison.Ordinal);
        Assert.Contains("split league op prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-ops", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leaguecontrol", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leaguecontrol prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)controls", playwright, StringComparison.Ordinal);
        Assert.Contains("split league controls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leaguecontrols", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leaguecontrols prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)control", playwright, StringComparison.Ordinal);
        Assert.Contains("split league control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-control", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leaguectrl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leaguectrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leaguectl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leaguectl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leaguectls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leaguectls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=leaguectrls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact leaguectrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)ctl", playwright, StringComparison.Ordinal);
        Assert.Contains("split league ctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-ctl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-ctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("split league ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)ctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("split league ctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-ctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-ctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league(?:%20|\\+)ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("split league ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=league-ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen league-ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityops", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityop", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityop prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-op", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-op prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityoperation", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityoperation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-operation", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-operation prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityoperations", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityoperations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-operations", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-operations prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)ops", playwright, StringComparison.Ordinal);
        Assert.Contains("split community ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)op", playwright, StringComparison.Ordinal);
        Assert.Contains("split community op prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-ops", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-ops prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communitycontrol", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communitycontrol prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)controls", playwright, StringComparison.Ordinal);
        Assert.Contains("split community controls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communitycontrols", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communitycontrols prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)control", playwright, StringComparison.Ordinal);
        Assert.Contains("split community control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-control", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-control prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityctl", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityctls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=communityctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("compact communityctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)ctl", playwright, StringComparison.Ordinal);
        Assert.Contains("split community ctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-ctl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-ctl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("split community ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-ctls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-ctls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)ctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("split community ctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-ctrls", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-ctrls prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community(?:%20|\\+)ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("split community ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=community-ctrl", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen community-ctrl prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=heat", playwright, StringComparison.Ordinal);
        Assert.Contains("heat continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=heats", playwright, StringComparison.Ordinal);
        Assert.Contains("heats continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=oppositions", playwright, StringComparison.Ordinal);
        Assert.Contains("oppositions prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=encounter", playwright, StringComparison.Ordinal);
        Assert.Contains("encounter prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=enemy", playwright, StringComparison.Ordinal);
        Assert.Contains("enemy prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hostile", playwright, StringComparison.Ordinal);
        Assert.Contains("hostile prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=adversary", playwright, StringComparison.Ordinal);
        Assert.Contains("adversary prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=threat", playwright, StringComparison.Ordinal);
        Assert.Contains("threat prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=opfor", playwright, StringComparison.Ordinal);
        Assert.Contains("opfor prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=opforce", playwright, StringComparison.Ordinal);
        Assert.Contains("opforce prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=opforces", playwright, StringComparison.Ordinal);
        Assert.Contains("opforces prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=opfors", playwright, StringComparison.Ordinal);
        Assert.Contains("opfors prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=op-force", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen op-force prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=op(?:%20|\\+)force", playwright, StringComparison.Ordinal);
        Assert.Contains("split op force prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=contact", playwright, StringComparison.Ordinal);
        Assert.Contains("contact continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=contacts", playwright, StringComparison.Ordinal);
        Assert.Contains("contacts continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=connection", playwright, StringComparison.Ordinal);
        Assert.Contains("connection continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=connections", playwright, StringComparison.Ordinal);
        Assert.Contains("connections continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=relationship", playwright, StringComparison.Ordinal);
        Assert.Contains("relationship continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=relationships", playwright, StringComparison.Ordinal);
        Assert.Contains("relationships continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=faction", playwright, StringComparison.Ordinal);
        Assert.Contains("faction continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=factions", playwright, StringComparison.Ordinal);
        Assert.Contains("factions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=journal", playwright, StringComparison.Ordinal);
        Assert.Contains("journal continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=journals", playwright, StringComparison.Ordinal);
        Assert.Contains("journals continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=sessionlog", playwright, StringComparison.Ordinal);
        Assert.Contains("sessionlog continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=sessionlogs", playwright, StringComparison.Ordinal);
        Assert.Contains("sessionlogs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=session(?:%20|\\+)logs", playwright, StringComparison.Ordinal);
        Assert.Contains("split session logs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=diary", playwright, StringComparison.Ordinal);
        Assert.Contains("diary continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=diaries", playwright, StringComparison.Ordinal);
        Assert.Contains("diaries continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=downtime", playwright, StringComparison.Ordinal);
        Assert.Contains("downtime continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=downtimes", playwright, StringComparison.Ordinal);
        Assert.Contains("downtimes continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=aftermath", playwright, StringComparison.Ordinal);
        Assert.Contains("aftermath continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=aftermaths", playwright, StringComparison.Ordinal);
        Assert.Contains("aftermaths continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=debrief", playwright, StringComparison.Ordinal);
        Assert.Contains("debrief continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=debriefs", playwright, StringComparison.Ordinal);
        Assert.Contains("debriefs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=debriefing", playwright, StringComparison.Ordinal);
        Assert.Contains("debriefing continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=debriefings", playwright, StringComparison.Ordinal);
        Assert.Contains("debriefings continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=outbrief", playwright, StringComparison.Ordinal);
        Assert.Contains("outbrief continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=outbriefs", playwright, StringComparison.Ordinal);
        Assert.Contains("outbriefs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=outbriefed", playwright, StringComparison.Ordinal);
        Assert.Contains("outbriefed continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=outbriefing", playwright, StringComparison.Ordinal);
        Assert.Contains("outbriefing continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=outbriefings", playwright, StringComparison.Ordinal);
        Assert.Contains("outbriefings continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out(?:%20|\\+)brief", playwright, StringComparison.Ordinal);
        Assert.Contains("split out brief continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out(?:%20|\\+)briefs", playwright, StringComparison.Ordinal);
        Assert.Contains("split out briefs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out(?:%20|\\+)briefed", playwright, StringComparison.Ordinal);
        Assert.Contains("split out briefed continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out(?:%20|\\+)briefing", playwright, StringComparison.Ordinal);
        Assert.Contains("split out briefing continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out(?:%20|\\+)briefings", playwright, StringComparison.Ordinal);
        Assert.Contains("split out briefings continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out-brief", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen out-brief continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out-briefs", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen out-briefs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out-briefed", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen out-briefed continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out-briefing", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen out-briefing continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=out-briefings", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen out-briefings continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postmortem", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postmortem continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)mortem", playwright, StringComparison.Ordinal);
        Assert.Contains("split post mortem continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-mortem", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-mortem continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postmortems", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postmortems continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)mortems", playwright, StringComparison.Ordinal);
        Assert.Contains("split post mortems continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-mortems", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-mortems continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postsession", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postsession continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)session", playwright, StringComparison.Ordinal);
        Assert.Contains("split post session continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-session", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-session continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postsessions", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postsessions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)sessions", playwright, StringComparison.Ordinal);
        Assert.Contains("split post sessions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-sessions", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-sessions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postrun", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postrun continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)run", playwright, StringComparison.Ordinal);
        Assert.Contains("split post run continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-run", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-run continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postruns", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postruns continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)runs", playwright, StringComparison.Ordinal);
        Assert.Contains("split post runs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-runs", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-runs continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postgame", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postgame continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=postgames", playwright, StringComparison.Ordinal);
        Assert.Contains("compact postgames continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)game", playwright, StringComparison.Ordinal);
        Assert.Contains("split post game continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post(?:%20|\\+)games", playwright, StringComparison.Ordinal);
        Assert.Contains("split post games continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-game", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-game continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=post-games", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen post-games continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=recap", playwright, StringComparison.Ordinal);
        Assert.Contains("recap continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=recaps", playwright, StringComparison.Ordinal);
        Assert.Contains("recaps continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=afteraction", playwright, StringComparison.Ordinal);
        Assert.Contains("compact afteraction continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=afteractions", playwright, StringComparison.Ordinal);
        Assert.Contains("compact afteractions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=afteractionreport", playwright, StringComparison.Ordinal);
        Assert.Contains("compact afteractionreport continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=afteractionreports", playwright, StringComparison.Ordinal);
        Assert.Contains("compact afteractionreports continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=afteractionreview", playwright, StringComparison.Ordinal);
        Assert.Contains("compact afteractionreview continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=afteractionreviews", playwright, StringComparison.Ordinal);
        Assert.Contains("compact afteractionreviews continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after(?:%20|\\+)action", playwright, StringComparison.Ordinal);
        Assert.Contains("split after action continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after(?:%20|\\+)actions", playwright, StringComparison.Ordinal);
        Assert.Contains("split after actions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after(?:%20|\\+)action(?:%20|\\+)report", playwright, StringComparison.Ordinal);
        Assert.Contains("split after action report continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after(?:%20|\\+)action(?:%20|\\+)reports", playwright, StringComparison.Ordinal);
        Assert.Contains("split after action reports continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after(?:%20|\\+)action(?:%20|\\+)review", playwright, StringComparison.Ordinal);
        Assert.Contains("split after action review continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after(?:%20|\\+)action(?:%20|\\+)reviews", playwright, StringComparison.Ordinal);
        Assert.Contains("split after action reviews continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after-action", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen after-action continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after-actions", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen after-actions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after-action(?:%20|\\+)report", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen after-action report continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after-action(?:%20|\\+)reports", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen after-action reports continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after-action(?:%20|\\+)review", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen after-action review continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=after-action(?:%20|\\+)reviews", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen after-action reviews continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=aar", playwright, StringComparison.Ordinal);
        Assert.Contains("compact aar continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=aars", playwright, StringComparison.Ordinal);
        Assert.Contains("compact aars continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=retro", playwright, StringComparison.Ordinal);
        Assert.Contains("compact retro continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=retros", playwright, StringComparison.Ordinal);
        Assert.Contains("compact retros continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=retrospective", playwright, StringComparison.Ordinal);
        Assert.Contains("compact retrospective continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=retrospectives", playwright, StringComparison.Ordinal);
        Assert.Contains("compact retrospectives continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hotwash", playwright, StringComparison.Ordinal);
        Assert.Contains("compact hotwash continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hotwashes", playwright, StringComparison.Ordinal);
        Assert.Contains("compact hotwashes continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hot(?:%20|\\+)wash", playwright, StringComparison.Ordinal);
        Assert.Contains("split hot wash continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hot(?:%20|\\+)washes", playwright, StringComparison.Ordinal);
        Assert.Contains("split hot washes continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hot-wash", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen hot-wash continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=hot-washes", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen hot-washes continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessonlearned", playwright, StringComparison.Ordinal);
        Assert.Contains("compact lessonlearned continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessonslearned", playwright, StringComparison.Ordinal);
        Assert.Contains("compact lessonslearned continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessonlearnt", playwright, StringComparison.Ordinal);
        Assert.Contains("compact lessonlearnt continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessonslearnt", playwright, StringComparison.Ordinal);
        Assert.Contains("compact lessonslearnt continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lesson(?:%20|\\+)learned", playwright, StringComparison.Ordinal);
        Assert.Contains("split lesson learned continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessons(?:%20|\\+)learned", playwright, StringComparison.Ordinal);
        Assert.Contains("split lessons learned continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lesson(?:%20|\\+)learnt", playwright, StringComparison.Ordinal);
        Assert.Contains("split lesson learnt continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessons(?:%20|\\+)learnt", playwright, StringComparison.Ordinal);
        Assert.Contains("split lessons learnt continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lesson-learnt", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen lesson-learnt continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessons-learnt", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen lessons-learnt continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lesson-learned", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen lesson-learned continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=lessons-learned", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen lessons-learned continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=return", playwright, StringComparison.Ordinal);
        Assert.Contains("return-loop continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=returns", playwright, StringComparison.Ordinal);
        Assert.Contains("returns continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=returnloop", playwright, StringComparison.Ordinal);
        Assert.Contains("compact returnloop continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=returnloops", playwright, StringComparison.Ordinal);
        Assert.Contains("compact returnloops continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=nextsession", playwright, StringComparison.Ordinal);
        Assert.Contains("compact nextsession continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=nextsessions", playwright, StringComparison.Ordinal);
        Assert.Contains("compact nextsessions continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=nextsessionreturn", playwright, StringComparison.Ordinal);
        Assert.Contains("compact nextsessionreturn continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=nextsessionreturns", playwright, StringComparison.Ordinal);
        Assert.Contains("compact nextsessionreturns continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'nextsessionreturnloop', 'compact nextsessionreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'nextsessionreturnloops', 'compact nextsessionreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'nextsessionreturnlane', 'compact nextsessionreturnlane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'nextsessionreturnlanes', 'compact nextsessionreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'nextsessionloop', 'compact nextsessionloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'nextsessionloops', 'compact nextsessionloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=sessionreturn", playwright, StringComparison.Ordinal);
        Assert.Contains("compact sessionreturn continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=sessionreturns", playwright, StringComparison.Ordinal);
        Assert.Contains("compact sessionreturns continuity prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'sessionreturnloop', 'compact sessionreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'sessionreturnloops', 'compact sessionreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'sessionreturnlane', 'compact sessionreturnlane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'sessionreturnlanes', 'compact sessionreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'session return loops', 'split session return loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'session-return-loops', 'hyphen session-return-loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'session return lane', 'split session return lane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'session return lanes', 'split session return lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'session-return-lane', 'hyphen session-return-lane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'session-return-lanes', 'hyphen session-return-lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return loop', 'split return loop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return loops', 'split return loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return-loops', 'hyphen return-loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return lane', 'split return lane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return lanes', 'split return lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return-lane', 'hyphen return-lane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'return-lanes', 'hyphen return-lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next session return loops', 'split next session return loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next-session-return-loops', 'hyphen next-session-return-loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next sessions return loops', 'split next sessions return loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next-sessions-return-loops', 'hyphen next-sessions-return-loops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next session return lanes', 'split next session return lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next-session-return-lanes', 'hyphen next-session-return-lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next sessions return lanes', 'split next sessions return lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'next-sessions-return-lanes', 'hyphen next-sessions-return-lanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarypacket', 'compact preplibrarypacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarypackets', 'compact preplibrarypackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarypkt', 'compact preplibrarypkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarypkts', 'compact preplibrarypkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarybrief', 'compact preplibrarybrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarybriefs', 'compact preplibrarybriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarybrf', 'compact preplibrarybrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'preplibrarybrfs', 'compact preplibrarybrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionpacket', 'compact oppositionpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionpackets', 'compact oppositionpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionpkt', 'compact oppositionpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionpkts', 'compact oppositionpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionbrief', 'compact oppositionbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionbriefs', 'compact oppositionbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionbrf', 'compact oppositionbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionbrfs', 'compact oppositionbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionspacket', 'compact oppositionspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionspackets', 'compact oppositionspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionspkt', 'compact oppositionspkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionspkts', 'compact oppositionspkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionsbrief', 'compact oppositionsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionsbriefs', 'compact oppositionsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionsbrf', 'compact oppositionsbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'oppositionsbrfs', 'compact oppositionsbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovepacket', 'compact rostermovepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovepackets', 'compact rostermovepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovepkt', 'compact rostermovepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovepkts', 'compact rostermovepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementpacket', 'compact rostermovementpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementpackets', 'compact rostermovementpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementpkt', 'compact rostermovementpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementpkts', 'compact rostermovementpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovebrief', 'compact rostermovebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovebriefs', 'compact rostermovebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementbrief', 'compact rostermovementbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementbriefs', 'compact rostermovementbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovebrf', 'compact rostermovebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovebrfs', 'compact rostermovebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementbrf', 'compact rostermovementbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostermovementbrfs', 'compact rostermovementbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovepacket', 'compact rostersmovepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovepackets', 'compact rostersmovepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovepkt', 'compact rostersmovepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovepkts', 'compact rostersmovepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementpacket', 'compact rostersmovementpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementpackets', 'compact rostersmovementpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementpkt', 'compact rostersmovementpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementpkts', 'compact rostersmovementpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovebrief', 'compact rostersmovebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovebriefs', 'compact rostersmovebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementbrief', 'compact rostersmovementbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementbriefs', 'compact rostersmovementbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovebrf', 'compact rostersmovebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovebrfs', 'compact rostersmovebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementbrf', 'compact rostersmovementbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'rostersmovementbrfs', 'compact rostersmovementbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolpacket', 'compact eventcontrolpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolpackets', 'compact eventcontrolpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolpkt', 'compact eventcontrolpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolpkts', 'compact eventcontrolpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolbrief', 'compact eventcontrolbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolbriefs', 'compact eventcontrolbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolbrf', 'compact eventcontrolbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolbrfs', 'compact eventcontrolbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolspacket', 'compact eventcontrolspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolspackets', 'compact eventcontrolspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolspkt', 'compact eventcontrolspkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolspkts', 'compact eventcontrolspkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolsbrief', 'compact eventcontrolsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolsbriefs', 'compact eventcontrolsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolsbrf', 'compact eventcontrolsbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolsbrfs', 'compact eventcontrolsbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolbrief', 'compact eventcontrolbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'eventcontrolbriefs', 'compact eventcontrolbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnpacket', 'compact campaignreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnpackets', 'compact campaignreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnpkt', 'compact campaignreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnpkts', 'compact campaignreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnbrief', 'compact campaignreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnbriefs', 'compact campaignreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnbrf', 'compact campaignreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignreturnbrfs', 'compact campaignreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnloop', 'compact campaignsreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnpacket', 'compact campaignsreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnpackets', 'compact campaignsreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnpkt', 'compact campaignsreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnpkts', 'compact campaignsreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnbrief', 'compact campaignsreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnbriefs', 'compact campaignsreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnbrf', 'compact campaignsreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignsreturnbrfs', 'compact campaignsreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnpacket', 'compact aftermathreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnpackets', 'compact aftermathreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnpkt', 'compact aftermathreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnpkts', 'compact aftermathreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnbrief', 'compact aftermathreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnbriefs', 'compact aftermathreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnbrf', 'compact aftermathreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathreturnbrfs', 'compact aftermathreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnpacket', 'compact aftermathsreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnpackets', 'compact aftermathsreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnpkt', 'compact aftermathsreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnpkts', 'compact aftermathsreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnbrief', 'compact aftermathsreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnbriefs', 'compact aftermathsreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnbrf', 'compact aftermathsreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsreturnbrfs', 'compact aftermathsreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnpacket', 'compact downtimereturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnpackets', 'compact downtimereturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnpkt', 'compact downtimereturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnpkts', 'compact downtimereturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnbrief', 'compact downtimereturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnbriefs', 'compact downtimereturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnbrf', 'compact downtimereturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimereturnbrfs', 'compact downtimereturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnloop', 'compact downtimesreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnpacket', 'compact downtimesreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnpackets', 'compact downtimesreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnpkt', 'compact downtimesreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnpkts', 'compact downtimesreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnbrief', 'compact downtimesreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnbriefs', 'compact downtimesreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnbrf', 'compact downtimesreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'downtimesreturnbrfs', 'compact downtimesreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnloop', 'compact diariesreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnloop', 'compact diaryreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnloops', 'compact diaryreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnlane', 'compact diaryreturnlane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnlanes', 'compact diaryreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnpacket', 'compact diariesreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnpackets', 'compact diariesreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnpkt', 'compact diariesreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnpkts', 'compact diariesreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnbrief', 'compact diariesreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnbriefs', 'compact diariesreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnbrf', 'compact diariesreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diariesreturnbrfs', 'compact diariesreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnpacket', 'compact diaryreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnpackets', 'compact diaryreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnpkt', 'compact diaryreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnpkts', 'compact diaryreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnbrief', 'compact diaryreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnbriefs', 'compact diaryreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnbrf', 'compact diaryreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diaryreturnbrfs', 'compact diaryreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnloop', 'compact contactsreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnloop', 'compact contactreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnloops', 'compact contactreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnlane', 'compact contactreturnlane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnlanes', 'compact contactreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnpacket', 'compact contactsreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnpackets', 'compact contactsreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnpkt', 'compact contactsreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnpkts', 'compact contactsreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnbrief', 'compact contactsreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnbriefs', 'compact contactsreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnbrf', 'compact contactsreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactsreturnbrfs', 'compact contactsreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnpacket', 'compact contactreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnpackets', 'compact contactreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnpkt', 'compact contactreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnpkts', 'compact contactreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnbrief', 'compact contactreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnbriefs', 'compact contactreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnbrf', 'compact contactreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'contactreturnbrfs', 'compact contactreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnloop', 'compact heatsreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnloop', 'compact heatreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnloops', 'compact heatreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnlane', 'compact heatreturnlane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnlanes', 'compact heatreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnpacket', 'compact heatsreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnpackets', 'compact heatsreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnpkt', 'compact heatsreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnpkts', 'compact heatsreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnbrief', 'compact heatsreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnbriefs', 'compact heatsreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnbrf', 'compact heatsreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatsreturnbrfs', 'compact heatsreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnpacket', 'compact heatreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnpackets', 'compact heatreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnpkt', 'compact heatreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnpkts', 'compact heatreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnbrief', 'compact heatreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnbriefs', 'compact heatreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnbrf', 'compact heatreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'heatreturnbrfs', 'compact heatreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diarycontactheatpacket', 'compact diarycontactheatpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diarycontactheatpackets', 'compact diarycontactheatpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diarycontactsheatpacket', 'compact diarycontactsheatpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'diarycontactsheatpackets', 'compact diarycontactsheatpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathdowntimepacket', 'compact aftermathdowntimepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathdowntimepackets', 'compact aftermathdowntimepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsdowntimepacket', 'compact aftermathsdowntimepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'aftermathsdowntimepackets', 'compact aftermathsdowntimepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinepacket', 'compact travelofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinepackets', 'compact travelofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinepkt', 'compact travelofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinepkts', 'compact travelofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinebrief', 'compact travelofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinebriefs', 'compact travelofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinebrf', 'compact travelofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelofflinebrfs', 'compact travelofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinepacket', 'compact travelsofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinepackets', 'compact travelsofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinepkt', 'compact travelsofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinepkts', 'compact travelsofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinebrief', 'compact travelsofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinebriefs', 'compact travelsofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinebrf', 'compact travelsofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelsofflinebrfs', 'compact travelsofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travel offline packet', 'split travel offline packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travel offline packets', 'split travel offline packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travel-offline-packet', 'hyphen travel-offline-packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travel-offline-packets', 'hyphen travel-offline-packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinepacket', 'compact mobileofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinepackets', 'compact mobileofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinepkt', 'compact mobileofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinepkts', 'compact mobileofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinebrief', 'compact mobileofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinebriefs', 'compact mobileofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinebrf', 'compact mobileofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinebrfs', 'compact mobileofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile offline packet', 'split mobile offline packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile offline packets', 'split mobile offline packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile-offline-packet', 'hyphen mobile-offline-packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile-offline-packets', 'hyphen mobile-offline-packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelpacket', 'compact safehousetravelpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelpackets', 'compact safehousetravelpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelpkt', 'compact safehousetravelpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelpkts', 'compact safehousetravelpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelbrief', 'compact safehousetravelbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelbriefs', 'compact safehousetravelbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelbrf', 'compact safehousetravelbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousetravelbrfs', 'compact safehousetravelbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelpacket', 'compact safehousestravelpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelpackets', 'compact safehousestravelpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelpkt', 'compact safehousestravelpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelpkts', 'compact safehousestravelpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelbrief', 'compact safehousestravelbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelbriefs', 'compact safehousestravelbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelbrf', 'compact safehousestravelbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousestravelbrfs', 'compact safehousestravelbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse travel packet', 'split safehouse travel packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse travel packets', 'split safehouse travel packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse-travel-packet', 'hyphen safehouse-travel-packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse-travel-packets', 'hyphen safehouse-travel-packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinepacket', 'compact safehouseofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinepackets', 'compact safehouseofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinepkt', 'compact safehouseofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinepkts', 'compact safehouseofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinebrief', 'compact safehouseofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinebriefs', 'compact safehouseofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinebrf', 'compact safehouseofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouseofflinebrfs', 'compact safehouseofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinepacket', 'compact safehousesofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinepackets', 'compact safehousesofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinepkt', 'compact safehousesofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinepkts', 'compact safehousesofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinebrief', 'compact safehousesofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinebriefs', 'compact safehousesofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinebrf', 'compact safehousesofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousesofflinebrfs', 'compact safehousesofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse offline packet', 'split safehouse offline packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse offline packets', 'split safehouse offline packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse-offline-packet', 'hyphen safehouse-offline-packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehouse-offline-packets', 'hyphen safehouse-offline-packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinepacket', 'compact mobilesafehouseofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinepackets', 'compact mobilesafehouseofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinepkt', 'compact mobilesafehouseofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinepkts', 'compact mobilesafehouseofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinebrief', 'compact mobilesafehouseofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinebriefs', 'compact mobilesafehouseofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinebrf', 'compact mobilesafehouseofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehouseofflinebrfs', 'compact mobilesafehouseofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinepacket', 'compact mobilesafehousesofflinepacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinepackets', 'compact mobilesafehousesofflinepackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinepkt', 'compact mobilesafehousesofflinepkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinepkts', 'compact mobilesafehousesofflinepkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinebrief', 'compact mobilesafehousesofflinebrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinebriefs', 'compact mobilesafehousesofflinebriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinebrf', 'compact mobilesafehousesofflinebrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousesofflinebrfs', 'compact mobilesafehousesofflinebrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile safehouse offline packet', 'split mobile safehouse offline packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile safehouse offline packets', 'split mobile safehouse offline packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile-safehouse-offline-packet', 'hyphen mobile-safehouse-offline-packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobile-safehouse-offline-packets', 'hyphen mobile-safehouse-offline-packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmopspacket', 'compact gmopspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmopspackets', 'compact gmopspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationpacket', 'compact gmoperationpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationpackets', 'compact gmoperationpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationspacket', 'compact gmoperationspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationspackets', 'compact gmoperationspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolpacket', 'compact gmcontrolpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolpackets', 'compact gmcontrolpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolspacket', 'compact gmcontrolspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolspackets', 'compact gmcontrolspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteropspacket', 'compact gamemasteropspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteropspackets', 'compact gamemasteropspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationpacket', 'compact gamemasteroperationpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationpackets', 'compact gamemasteroperationpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationspacket', 'compact gamemasteroperationspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationspackets', 'compact gamemasteroperationspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolpacket', 'compact gamemastercontrolpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolpackets', 'compact gamemastercontrolpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolspacket', 'compact gamemastercontrolspacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolspackets', 'compact gamemastercontrolspackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmopsbrief', 'compact gmopsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmopsbriefs', 'compact gmopsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationbrief', 'compact gmoperationbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationbriefs', 'compact gmoperationbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationsbrief', 'compact gmoperationsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmoperationsbriefs', 'compact gmoperationsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolbrief', 'compact gmcontrolbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolbriefs', 'compact gmcontrolbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolsbrief', 'compact gmcontrolsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gmcontrolsbriefs', 'compact gmcontrolsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteropsbrief', 'compact gamemasteropsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteropsbriefs', 'compact gamemasteropsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationbrief', 'compact gamemasteroperationbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationbriefs', 'compact gamemasteroperationbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationsbrief', 'compact gamemasteroperationsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasteroperationsbriefs', 'compact gamemasteroperationsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolbrief', 'compact gamemastercontrolbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolbriefs', 'compact gamemastercontrolbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolsbrief', 'compact gamemastercontrolsbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemastercontrolsbriefs', 'compact gamemastercontrolsbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasterctls', 'compact gamemasterctls continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'gamemasterctrls', 'compact gamemasterctrls continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'workspacev4', 'compact workspacev4 continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignworkspacev4', 'compact campaignworkspacev4 continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'workspacev4packet', 'compact workspacev4packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'workspacev4packets', 'compact workspacev4packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'workspacev4brief', 'compact workspacev4brief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'workspacev4briefs', 'compact workspacev4briefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignworkspacev4packet', 'compact campaignworkspacev4packet continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignworkspacev4packets', 'compact campaignworkspacev4packets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignworkspacev4brief', 'compact campaignworkspacev4brief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignworkspacev4briefs', 'compact campaignworkspacev4briefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionreturnlanes', 'compact mobilecompanionreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionsreturnloop', 'compact mobilecompanionsreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionsreturnloops', 'compact mobilecompanionsreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionreturnpacket', 'compact mobilecompanionreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionsreturnpackets', 'compact mobilecompanionsreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionreturnpkt', 'compact mobilecompanionreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionsreturnpkts', 'compact mobilecompanionsreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionreturnbrief', 'compact mobilecompanionreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionsreturnbriefs', 'compact mobilecompanionsreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionreturnbrf', 'compact mobilecompanionreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilecompanionsreturnbrfs', 'compact mobilecompanionsreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionreturnlane', 'compact campaignmobilecompanionreturnlane continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionreturnlanes', 'compact campaignmobilecompanionreturnlanes continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionsreturnloop', 'compact campaignmobilecompanionsreturnloop continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionsreturnloops', 'compact campaignmobilecompanionsreturnloops continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionreturnpacket', 'compact campaignmobilecompanionreturnpacket continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionsreturnpackets', 'compact campaignmobilecompanionsreturnpackets continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionreturnpkt', 'compact campaignmobilecompanionreturnpkt continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionsreturnpkts', 'compact campaignmobilecompanionsreturnpkts continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionreturnbrief', 'compact campaignmobilecompanionreturnbrief continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionsreturnbriefs', 'compact campaignmobilecompanionsreturnbriefs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionreturnbrf', 'compact campaignmobilecompanionreturnbrf continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'campaignmobilecompanionsreturnbrfs', 'compact campaignmobilecompanionsreturnbrfs continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=memory", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-memory prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=memories", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-memories prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=archive", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-archive prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=archives", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-archives prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=history", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-history prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=histories", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-histories prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=timeline", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-timeline prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=timelines", playwright, StringComparison.Ordinal);
        Assert.Contains("campaign-timelines prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=ledger", playwright, StringComparison.Ordinal);
        Assert.Contains("memory-ledger prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=ledgers", playwright, StringComparison.Ordinal);
        Assert.Contains("memory-ledgers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster", playwright, StringComparison.Ordinal);
        Assert.Contains("roster-movement prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rostermove", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rostermove query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rostermoves", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rostermoves prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rostershift", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rostershift prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rostershifts", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rostershifts prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewmove", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewmove prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewmoves", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewmoves prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewshift", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewshift prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewshifts", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewshifts prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewswap", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewswap prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewswaps", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewswaps prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rostertransfer", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rostertransfer prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rostertransfers", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rostertransfers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rosterswap", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rosterswap prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rosterswaps", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rosterswaps prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rosterhandoff", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rosterhandoff prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rosterhandoffs", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rosterhandoffs prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rosterhandover", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rosterhandover prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=rosterhandovers", playwright, StringComparison.Ordinal);
        Assert.Contains("compact rosterhandovers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewhandoff", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewhandoff prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewhandoffs", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewhandoffs prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewtransfer", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewtransfer prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crewtransfers", playwright, StringComparison.Ordinal);
        Assert.Contains("compact crewtransfers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew(?:%20|\\+)transfer", playwright, StringComparison.Ordinal);
        Assert.Contains("split crew transfer prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew(?:%20|\\+)transfers", playwright, StringComparison.Ordinal);
        Assert.Contains("split crew transfers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew(?:%20|\\+)handoff", playwright, StringComparison.Ordinal);
        Assert.Contains("split crew handoff prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew(?:%20|\\+)handoffs", playwright, StringComparison.Ordinal);
        Assert.Contains("split crew handoffs prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew(?:%20|\\+)move", playwright, StringComparison.Ordinal);
        Assert.Contains("split crew move prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew(?:%20|\\+)moves", playwright, StringComparison.Ordinal);
        Assert.Contains("split crew moves prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)transfer", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster transfer prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)transfers", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster transfers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)handoff", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster handoff prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)handoffs", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster handoffs prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)handover", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster handover prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)handovers", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster handovers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)move", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster move prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster(?:%20|\\+)moves", playwright, StringComparison.Ordinal);
        Assert.Contains("split roster moves prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster-move", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen roster-move prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew-move", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen crew-move prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster-transfer", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen roster-transfer prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew-transfer", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen crew-transfer prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster-handoff", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen roster-handoff prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster-handover", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen roster-handover prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=roster-handovers", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen roster-handovers prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=crew-handoff", playwright, StringComparison.Ordinal);
        Assert.Contains("hyphen crew-handoff prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=preplaunch", playwright, StringComparison.Ordinal);
        Assert.Contains("compact preplaunch prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=preplaunches", playwright, StringComparison.Ordinal);
        Assert.Contains("compact preplaunches prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=travelprefetch", playwright, StringComparison.Ordinal);
        Assert.Contains("compact travelprefetch prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("?prepQuery=travelprefetches", playwright, StringComparison.Ordinal);
        Assert.Contains("compact travelprefetches prep query", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'offlinereadiness', 'compact offlinereadiness continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'offlinereadinesses', 'compact offlinereadinesses continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'offlinecache', 'compact offlinecache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'offlinecaches', 'compact offlinecaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'stalecache', 'compact stalecache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'stalecaches', 'compact stalecaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'staleofflinecache', 'compact staleofflinecache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'staleofflinecaches', 'compact staleofflinecaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelreadiness', 'compact travelreadiness continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelreadinesses', 'compact travelreadinesses continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelcache', 'compact travelcache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'travelcaches', 'compact travelcaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousereadiness', 'compact safehousereadiness continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousereadinesses', 'compact safehousereadinesses continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousecache', 'compact safehousecache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'safehousecaches', 'compact safehousecaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinereadiness', 'compact mobileofflinereadiness continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinereadinesses', 'compact mobileofflinereadinesses continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobiletravelreadiness', 'compact mobiletravelreadiness continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobiletravelreadinesses', 'compact mobiletravelreadinesses continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinecache', 'compact mobileofflinecache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobileofflinecaches', 'compact mobileofflinecaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobiletravelcache', 'compact mobiletravelcache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobiletravelcaches', 'compact mobiletravelcaches continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousereadiness', 'compact mobilesafehousereadiness continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousereadinesses', 'compact mobilesafehousereadinesses continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousecache', 'compact mobilesafehousecache continuity prep');", playwright, StringComparison.Ordinal);
        Assert.Contains("assertWorkspacePrepQuerySearch(page, pageErrors, 'mobilesafehousecaches', 'compact mobilesafehousecaches continuity prep');", playwright, StringComparison.Ordinal);
    }

    [Fact]
    public void PortalE2EUsesReverseProxiedLocalEdgeHeaders()
    {
        string shellPath = RepoPaths.FromRoot("scripts", "e2e-portal.sh");
        string nodePath = RepoPaths.FromRoot("scripts", "e2e-portal.cjs");

        string shell = File.ReadAllText(shellPath);
        string node = File.ReadAllText(nodePath);

        Assert.Contains("wait_for_portal_edge", shell, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_PORTAL_PUBLIC_HOST", shell, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_PORTAL_FORWARDED_PROTO", shell, StringComparison.Ordinal);
        Assert.Contains("X-Forwarded-Proto", node, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_PORTAL_PUBLIC_HOST", node, StringComparison.Ordinal);
        Assert.Contains("CHUMMER_PORTAL_FORWARDED_PROTO", node, StringComparison.Ordinal);
    }

    [Fact]
    public void HubRequestObservabilityIsWiredIntoProgramAndVerification()
    {
        string programPath = RepoPaths.FromRoot("Chummer.Run.Api", "Program.cs");
        string identityProgramPath = RepoPaths.FromRoot("Chummer.Run.Identity", "Program.cs");
        string verificationProgramPath = RepoPaths.FromRoot("tests", "RunServicesVerification", "Program.cs");
        string backlogPath = RepoPaths.FromRoot("docs", "MIGRATION_BACKLOG.md");
        string middlewarePath = RepoPaths.FromRoot("Chummer.Run.Api", "HubRequestObservabilityMiddleware.cs");

        string program = File.ReadAllText(programPath);
        string identityProgram = File.ReadAllText(identityProgramPath);
        string verificationProgram = File.ReadAllText(verificationProgramPath);
        string backlog = File.ReadAllText(backlogPath);
        string middleware = File.ReadAllText(middlewarePath);

        Assert.Contains("AddHubRequestObservability", program, StringComparison.Ordinal);
        Assert.Contains("UseHubRequestObservability", program, StringComparison.Ordinal);
        Assert.Contains("HubRequestObservabilityVerification.RunAsync", verificationProgram, StringComparison.Ordinal);
        Assert.Contains("MIG-091", backlog, StringComparison.Ordinal);
        Assert.Contains("Response.OnStarting", middleware, StringComparison.Ordinal);
        Assert.Contains("IDENTITY_ENABLE_HTTPS_REDIRECTION", identityProgram, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicEdgeProgramForcesNoStoreHeadersForProofAndInstallRoutes()
    {
        string programPath = RepoPaths.FromRoot("Chummer.Run.Api", "Program.cs");
        string program = File.ReadAllText(programPath);

        Assert.Contains("RequiresNoStoreHeaders", program, StringComparison.Ordinal);
        Assert.Contains("/downloads/proof/windows", program, StringComparison.Ordinal);
        Assert.Contains("/downloads/install", program, StringComparison.Ordinal);
        Assert.Contains("/install-", program, StringComparison.Ordinal);
        Assert.Contains("Cache-Control", program, StringComparison.Ordinal);
        Assert.Contains("CDN-Cache-Control", program, StringComparison.Ordinal);
        Assert.Contains("Cloudflare-CDN-Cache-Control", program, StringComparison.Ordinal);
        Assert.Contains("Surrogate-Control", program, StringComparison.Ordinal);
        Assert.Contains("no-store, max-age=0", program, StringComparison.Ordinal);
        Assert.Contains("no-cache", program, StringComparison.Ordinal);
    }

    [Fact]
    public void NowPageKeepsCampaignOsDetailOffPrimaryInstallSurface()
    {
        string serviceCollectionPath = RepoPaths.FromRoot("Chummer.Run.Api", "ServiceCollectionBoundedContextExtensions.cs");
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string viewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Now.cshtml");

        string serviceCollection = File.ReadAllText(serviceCollectionPath);
        string controller = File.ReadAllText(controllerPath);
        string viewModel = File.ReadAllText(viewModelPath);
        string view = File.ReadAllText(viewPath);

        Assert.Contains("CampaignOsLocalProofService", serviceCollection, StringComparison.Ordinal);
        Assert.Contains("CampaignOsProof: _campaignOsProof.LoadProof()", controller, StringComparison.Ordinal);
        Assert.Contains("CampaignOsLocalProofSnapshot? CampaignOsProof", viewModel, StringComparison.Ordinal);
        Assert.DoesNotContain("Campaign OS", view, StringComparison.Ordinal);
        Assert.DoesNotContain("Source-backed local", view, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicTrustPagesSurfaceWeeklyPulseAndCaution()
    {
        string serviceCollectionPath = RepoPaths.FromRoot("Chummer.Run.Api", "ServiceCollectionBoundedContextExtensions.cs");
        string servicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "PublicTrustPulseService.cs");
        string pulseArtifactServicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "WeeklyProductPulseArtifactService.cs");
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string partialPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "Shared", "_PublicTrustPulsePanel.cshtml");
        string bodyPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "Shared", "_PublicTrustPulseBody.cshtml");

        string serviceCollection = File.ReadAllText(serviceCollectionPath);
        string service = File.ReadAllText(servicePath);
        string pulseArtifactService = File.ReadAllText(pulseArtifactServicePath);
        string controller = File.ReadAllText(controllerPath);
        string viewModel = File.ReadAllText(viewModelPath);
        string partial = File.ReadAllText(partialPath);
        string body = File.ReadAllText(bodyPath);

        Assert.Contains("PublicTrustPulseService", serviceCollection, StringComparison.Ordinal);
        Assert.Contains("WeeklyProductPulseArtifactService", serviceCollection, StringComparison.Ordinal);
        Assert.Contains("LoadWeeklyPulseJson", service, StringComparison.Ordinal);
        Assert.Contains("WEEKLY_PRODUCT_PULSE.generated.json", pulseArtifactService, StringComparison.Ordinal);
        Assert.Contains("BuildPublicTrustPulsePanel", controller, StringComparison.Ordinal);
        Assert.Contains("PublicTrustPulsePanelViewModel? TrustPulse", viewModel, StringComparison.Ordinal);
        Assert.Contains("PublicTrustPulseTrendPointViewModel", viewModel, StringComparison.Ordinal);
        Assert.Contains("Weekly trust pulse", partial, StringComparison.Ordinal);
        Assert.Contains("trust-pulse-trend", body, StringComparison.Ordinal);
        Assert.Contains("Current caution", controller, StringComparison.Ordinal);
        Assert.Contains("Closure health", controller, StringComparison.Ordinal);
        Assert.Contains("Progress trend", controller, StringComparison.Ordinal);
        Assert.Contains("BuildTrustPulseProgressTrendSummary", controller, StringComparison.Ordinal);
        Assert.Contains("BuildTrustPulseTrendSamples", controller, StringComparison.Ordinal);
        Assert.Contains("BuildTrustPulseClosureHealthSummary", controller, StringComparison.Ordinal);
    }

    [Fact]
    public void SignedInTrustPanelStaysOnAccountAndContextHeavySurfaces()
    {
        string serviceCollectionPath = RepoPaths.FromRoot("Chummer.Run.Api", "ServiceCollectionBoundedContextExtensions.cs");
        string servicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "SignedInTrustStatusService.cs");
        string publicControllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string accountControllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "AccountsController.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string accountViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "Accounts", "Account.cshtml");

        string serviceCollection = File.ReadAllText(serviceCollectionPath);
        string service = File.ReadAllText(servicePath);
        string publicController = File.ReadAllText(publicControllerPath);
        string accountController = File.ReadAllText(accountControllerPath);
        string viewModel = File.ReadAllText(viewModelPath);
        string accountView = File.ReadAllText(accountViewPath);
        string landingView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Landing.cshtml"));
        string faqView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Faq.cshtml"));
        string storyView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "ProductStory.cshtml"));
        string participateView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Participate.cshtml"));
        string horizonsView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Horizons.cshtml"));
        string shelfView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Shelf.cshtml"));
        string featureDetailView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "FeatureDetail.cshtml"));
        string downloadsView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Downloads.cshtml"));
        string downloadDispatchView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "DownloadDispatch.cshtml"));
        string homeView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Home.cshtml"));
        string nowView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Now.cshtml"));
        string supportSubmittedView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "SupportSubmitted.cshtml"));
        string publicCreatorPublicationView = File.ReadAllText(RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "PublicCreatorPublication.cshtml"));

        Assert.Contains("SignedInTrustStatusService", serviceCollection, StringComparison.Ordinal);
        Assert.Contains("Who can get it now", service, StringComparison.Ordinal);
        Assert.Contains("BuildRecommendedInstallAction(releaseExperience", service, StringComparison.Ordinal);
        Assert.Contains("_signedInTrustStatus.Build", publicController, StringComparison.Ordinal);
        Assert.Contains("_signedInTrustStatus.Build", accountController, StringComparison.Ordinal);
        Assert.Contains("SignedInTrustStatusPanelViewModel? SignedInTrustStatus", viewModel, StringComparison.Ordinal);
        Assert.Contains("SignedInTrustStatusPanelViewModel? SignedInStatus", viewModel, StringComparison.Ordinal);
        Assert.Contains("_SignedInTrustStatusPanel.cshtml", accountView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", horizonsView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", shelfView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", featureDetailView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", landingView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", faqView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", storyView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", participateView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", downloadsView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", downloadDispatchView, StringComparison.Ordinal);
        Assert.Contains("_SignedInTrustStatusPanel.cshtml", homeView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", nowView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", supportSubmittedView, StringComparison.Ordinal);
        Assert.DoesNotContain("_SignedInTrustStatusPanel.cshtml", publicCreatorPublicationView, StringComparison.Ordinal);
    }

    [Fact]
    public void LandingPageContextualizesGuestPreviewInstallLinks()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string servicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "ReleaseSelectionService.cs");
        string landingViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Landing.cshtml");

        string controller = File.ReadAllText(controllerPath);
        string service = File.ReadAllText(servicePath);
        string landingView = File.ReadAllText(landingViewPath);

        Assert.Contains("var releaseExperience = _releaseSelection.BuildExperience", controller, StringComparison.Ordinal);
        Assert.Contains("_releaseSelection.BuildPublicPrimaryAction(", controller, StringComparison.Ordinal);
        Assert.Contains("Request.Headers.UserAgent.ToString()", controller, StringComparison.Ordinal);
        Assert.Contains("release.Recommended.ActionLabel", service, StringComparison.Ordinal);
        Assert.Contains("release.Recommended.DispatchHref", service, StringComparison.Ordinal);
        Assert.Contains("guestNeedsInstallGate", landingView, StringComparison.Ordinal);
        Assert.Contains("contextualHeroPrimaryHref", landingView, StringComparison.Ordinal);
        Assert.Contains("contextualProductPrimaryAction", landingView, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref", landingView, StringComparison.Ordinal);
        Assert.Contains("release.GuestGatePrimaryHref", landingView, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicStoryNowAndStatusPagesContextualizeGuestPreviewInstallLinks()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string storyViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "ProductStory.cshtml");
        string nowViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Now.cshtml");
        string statusViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Status.cshtml");

        string controller = File.ReadAllText(controllerPath);
        string viewModels = File.ReadAllText(viewModelPath);
        string storyView = File.ReadAllText(storyViewPath);
        string nowView = File.ReadAllText(nowViewPath);
        string statusView = File.ReadAllText(statusViewPath);

        Assert.Contains("ReleaseExperience: releaseExperience", controller, StringComparison.Ordinal);
        Assert.Contains("StoryPageViewModel(", viewModels, StringComparison.Ordinal);
        Assert.Contains("ReleaseExperienceViewModel ReleaseExperience", viewModels, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref", storyView, StringComparison.Ordinal);
        Assert.Contains("Model.ReleaseExperience.GuestGatePrimaryHref", storyView, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref", nowView, StringComparison.Ordinal);
        Assert.Contains("Model.ReleaseExperience.GuestGatePrimaryHref", nowView, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref", statusView, StringComparison.Ordinal);
        Assert.Contains("Model.ReleaseExperience.GuestGatePrimaryHref", statusView, StringComparison.Ordinal);
    }

    [Fact]
    public void SignedInHomePageContextualizesInstallLinksThroughReleaseExperience()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string homeViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Home.cshtml");

        string controller = File.ReadAllText(controllerPath);
        string viewModel = File.ReadAllText(viewModelPath);
        string homeView = File.ReadAllText(homeViewPath);

        Assert.Contains("ReleaseExperience: releaseExperience", controller, StringComparison.Ordinal);
        Assert.Contains("BuildHomePrimaryAction(experience, campaignSpine, installLinking, releaseExperience)", controller, StringComparison.Ordinal);
        Assert.Contains("ReleaseExperienceViewModel ReleaseExperience", viewModel, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref(Model.PrimaryAction.Href)", homeView, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref(supportCase.PrimaryActionHref)", homeView, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref(\"/downloads\")", homeView, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicHelpFaqAndTrustPulseContextualizeGuestPreviewInstallLinks()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string faqViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "Faq.cshtml");
        string trustViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "PublicLanding", "TrustPage.cshtml");

        string controller = File.ReadAllText(controllerPath);
        string faqView = File.ReadAllText(faqViewPath);
        string trustView = File.ReadAllText(trustViewPath);

        Assert.Contains("releaseExperience.GuestGatePrimaryHref", controller, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref", faqView, StringComparison.Ordinal);
        Assert.Contains("Model.Chrome.PublicPrimaryCta", faqView, StringComparison.Ordinal);
        Assert.Contains("ContextualPreviewHref", trustView, StringComparison.Ordinal);
        Assert.Contains("Model.Chrome.PublicPrimaryCta", trustView, StringComparison.Ordinal);
    }

    [Fact]
    public void AccountWorkSurfaceShowsDecisionReceiptHistoryAndResumabilityNotes()
    {
        string accountViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "Accounts", "Account.cshtml");
        string accountView = File.ReadAllText(accountViewPath);

        Assert.Contains("Decision receipt history", accountView, StringComparison.Ordinal);
        Assert.Contains("PublicationDecisionOutcomeSummary", accountView, StringComparison.Ordinal);
        Assert.Contains("PublicationResumabilitySummary", accountView, StringComparison.Ordinal);
        Assert.Contains("AutomationOutcomeSummary", accountView, StringComparison.Ordinal);
        Assert.Contains("AutomationResumabilitySummary", accountView, StringComparison.Ordinal);
        Assert.Contains("Recent governed prep launches", accountView, StringComparison.Ordinal);
        Assert.Contains("Recent travel prefetch receipts", accountView, StringComparison.Ordinal);
        Assert.Contains("Recent aftermath and replay packages", accountView, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicProgressControllerPublishesWeeklyPulseArtifact()
    {
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicProgressController.cs");
        string servicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "PublicProgressService.cs");
        string auditPath = RepoPaths.FromRoot("scripts", "hub-live-audit.py");

        string controller = File.ReadAllText(controllerPath);
        string service = File.ReadAllText(servicePath);
        string audit = File.ReadAllText(auditPath);

        Assert.Contains("/api/public/weekly-pulse", controller, StringComparison.Ordinal);
        Assert.Contains("LoadWeeklyPulseJson", service, StringComparison.Ordinal);
        Assert.Contains("/api/public/weekly-pulse", audit, StringComparison.Ordinal);
        Assert.Contains("chummer.weekly_product_pulse", audit, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicTrustPagesPublishPrivacyBoundaryArtifact()
    {
        string serviceCollectionPath = RepoPaths.FromRoot("Chummer.Run.Api", "ServiceCollectionBoundedContextExtensions.cs");
        string controllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicProgressController.cs");
        string landingControllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "PublicLandingController.cs");
        string accountControllerPath = RepoPaths.FromRoot("Chummer.Run.Api", "Controllers", "AccountsController.cs");
        string servicePath = RepoPaths.FromRoot("Chummer.Run.Api", "Services", "PublicPrivacyBoundaryService.cs");
        string viewModelPath = RepoPaths.FromRoot("Chummer.Run.Api", "ViewModels", "SiteViewModels.cs");
        string partialPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "Shared", "_PrivacyBoundaryPanel.cshtml");
        string accountViewPath = RepoPaths.FromRoot("Chummer.Run.Api", "Views", "Accounts", "Account.cshtml");
        string auditPath = RepoPaths.FromRoot("scripts", "hub-live-audit.py");

        string serviceCollection = File.ReadAllText(serviceCollectionPath);
        string controller = File.ReadAllText(controllerPath);
        string landingController = File.ReadAllText(landingControllerPath);
        string accountController = File.ReadAllText(accountControllerPath);
        string service = File.ReadAllText(servicePath);
        string viewModel = File.ReadAllText(viewModelPath);
        string partial = File.ReadAllText(partialPath);
        string accountView = File.ReadAllText(accountViewPath);
        string audit = File.ReadAllText(auditPath);

        Assert.Contains("PublicPrivacyBoundaryService", serviceCollection, StringComparison.Ordinal);
        Assert.Contains("/api/public/privacy-boundaries", controller, StringComparison.Ordinal);
        Assert.Contains("BuildPanel(\"privacy\")", landingController, StringComparison.Ordinal);
        Assert.Contains("BuildPanel(\"help\")", landingController, StringComparison.Ordinal);
        Assert.Contains("BuildPanel(\"contact\")", landingController, StringComparison.Ordinal);
        Assert.Contains("BuildPanel(\"account\")", accountController, StringComparison.Ordinal);
        Assert.Contains("PRIVACY_AND_RETENTION_BOUNDARIES.md", service, StringComparison.Ordinal);
        Assert.Contains("PUBLIC_TRUST_CONTENT.yaml", service, StringComparison.Ordinal);
        Assert.Contains("PrivacyBoundaryPanelViewModel? PrivacyBoundary", viewModel, StringComparison.Ordinal);
        Assert.Contains("Retention window:", partial, StringComparison.Ordinal);
        Assert.Contains("Model.PrivacyBoundary", accountView, StringComparison.Ordinal);
        Assert.Contains("/api/public/privacy-boundaries", audit, StringComparison.Ordinal);
        Assert.Contains("chummer.public_privacy_boundaries", audit, StringComparison.Ordinal);
    }

    [Fact]
    public void ReleaseWorkflowPublishesApiAndDownloadsMirrorArtifacts()
    {
        string workflowPath = RepoPaths.FromRoot(".github", "workflows", "desktop-downloads-matrix.yml");
        string docsPath = RepoPaths.FromRoot("docs", "ACTIVE_HEAD_RELEASE_ARTIFACTS.md");

        string workflow = File.ReadAllText(workflowPath);
        string docs = File.ReadAllText(docsPath);

        Assert.Contains("- main", workflow, StringComparison.Ordinal);
        Assert.Contains("name: Public Edge Release Artifacts", workflow, StringComparison.Ordinal);
        Assert.Contains("name: release-api-portable", workflow, StringComparison.Ordinal);
        Assert.Contains("Stage public downloads mirror", workflow, StringComparison.Ordinal);
        Assert.Contains("desktop-download-bundle", workflow, StringComparison.Ordinal);
        Assert.DoesNotContain("Chummer.Avalonia/Chummer.Avalonia.csproj", workflow, StringComparison.Ordinal);
        Assert.DoesNotContain("Chummer.Blazor.Desktop/Chummer.Blazor.Desktop.csproj", workflow, StringComparison.Ordinal);
        Assert.Contains("release-api-portable", docs, StringComparison.Ordinal);
        Assert.Contains("checked-in public download mirror", docs, StringComparison.Ordinal);
        Assert.Contains("desktop-download-bundle", docs, StringComparison.Ordinal);
    }

    [Fact]
    public void PublicEdgeGuardrailsDoNotReferenceMissingPortalProject()
    {
        string workflowPath = RepoPaths.FromRoot(".github", "workflows", "docker-architecture-guardrails.yml");
        string composePath = RepoPaths.FromRoot("legacy", "tooling", "docker", "docker-compose.yml");
        string runbookPath = RepoPaths.FromRoot("scripts", "runbook.sh");
        string migrationLoopPath = RepoPaths.FromRoot("scripts", "migration-loop.sh");
        string backlogPath = RepoPaths.FromRoot("docs", "MIGRATION_BACKLOG.md");

        string workflow = File.ReadAllText(workflowPath);
        string compose = File.ReadAllText(composePath);
        string runbook = File.ReadAllText(runbookPath);
        string migrationLoop = File.ReadAllText(migrationLoopPath);
        string backlog = File.ReadAllText(backlogPath);

        Assert.DoesNotContain("Chummer.Portal/Chummer.Portal.csproj", workflow, StringComparison.Ordinal);
        Assert.DoesNotContain("Chummer.Portal/appsettings.json", runbook, StringComparison.Ordinal);
        Assert.Contains("Chummer.Run.Api/Chummer.Run.Api.csproj", workflow, StringComparison.Ordinal);
        Assert.Contains("Chummer.Run.Api/Dockerfile", compose, StringComparison.Ordinal);
        Assert.Contains("chummer.run:host-gateway", compose, StringComparison.Ordinal);
        Assert.Contains("PublicLandingController.cs", runbook, StringComparison.Ordinal);
        Assert.DoesNotContain("chummer-blazor", migrationLoop, StringComparison.Ordinal);
        Assert.Contains("docker-compose.public-edge.yml", migrationLoop, StringComparison.Ordinal);
        Assert.DoesNotContain("CHUMMER_PORTAL_DEV_AUTH_ENABLED", backlog, StringComparison.Ordinal);
        Assert.DoesNotContain("CHUMMER_PORTAL_REQUIRE_AUTH", backlog, StringComparison.Ordinal);
        Assert.Contains("Chummer.Run.Api", backlog, StringComparison.Ordinal);
    }

    [Fact]
    public void WorkspaceBenchmarkGuardrailsAreOwnedByCoreEngineRepo()
    {
        string backlogPath = RepoPaths.FromRoot("docs", "MIGRATION_BACKLOG.md");
        string boundaryPath = RepoPaths.FromRoot("docs", "HOSTED_BOUNDARY.md");
        string coreEngineRoot = Path.GetFullPath(Path.Combine(RepoPaths.Root, "..", "chummer-core-engine"));
        string benchmarkWorkflowPath = Path.Combine(coreEngineRoot, ".github", "workflows", "benchmark-guardrails.yml");
        string benchmarkBudgetPath = Path.Combine(coreEngineRoot, "Chummer.Benchmarks", "workspace-benchmark-budgets.json");

        string backlog = File.ReadAllText(backlogPath);
        string boundary = File.ReadAllText(boundaryPath);

        Assert.Contains("- [x] `MIG-095`", backlog, StringComparison.Ordinal);
        Assert.Contains("../chummer-core-engine/Chummer.Benchmarks", backlog, StringComparison.Ordinal);
        Assert.Contains("Chummer.Benchmarks", boundary, StringComparison.Ordinal);
        Assert.True(File.Exists(benchmarkWorkflowPath), "Core engine owner repo should publish the benchmark CI workflow.");
        Assert.True(File.Exists(benchmarkBudgetPath), "Core engine owner repo should publish benchmark budgets.");
    }
}
